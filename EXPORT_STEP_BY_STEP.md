# 📋 Пошаговые команды для экспорта

## 🎯 Все команды по порядку:

### 1️⃣ Авторизация в Supabase
```powershell
npx supabase login
```
**Что произойдет:**
- Нажмите Enter
- Откроется браузер
- Войдите: `nazardubnak@gmail.com` / `Dn2907200!`
- Подтвердите доступ

---

### 2️⃣ Просмотр проектов (чтобы найти project-ref)
```powershell
npx supabase projects list
```
**Результат:** Список всех ваших проектов с project-ref

---

### 3️⃣ Связывание с проектом
```powershell
npx supabase link --project-ref ВАШ_PROJECT_REF
```
**Замените `ВАШ_PROJECT_REF`** на реальный ID из команды выше или из URL.

**Пример:**
```powershell
npx supabase link --project-ref mtpawypaihmwrngirnxa
```

---

### 4️⃣ Экспорт полного дампа
```powershell
npx supabase db dump --schema public,auth,storage -f full_dump.sql
```
**Результат:** Файл `full_dump.sql` со всеми миграциями

---

## 🔍 Где взять Project-Ref:

### Вариант A: Из URL браузера
1. Откройте https://supabase.com
2. Войдите и откройте проект "Дневник подопечного"
3. Посмотрите URL:
   ```
   https://supabase.com/dashboard/project/mtpawypaihmwrngirnxa
   ```
   `mtpawypaihmwrngirnxa` - это project-ref

### Вариант B: Из Settings
1. В проекте: **Settings** → **General**
2. Найдите **Reference ID**

### Вариант C: Из команды
После `npx supabase login` выполните:
```powershell
npx supabase projects list
```
В выводе будет project-ref для каждого проекта.

---

## ✅ После экспорта:

Пришлите файл `full_dump.sql` — применю все миграции на новом сервере!

