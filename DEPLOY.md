# Инструкция по деплою на хостинг

## ✅ Подготовка проекта

Проект полностью готов к деплою! Структура настроена правильно.

### Что уже настроено:

- ✅ Vite конфигурация для production сборки
- ✅ React Router настроен (BrowserRouter)
- ✅ Все зависимости в package.json
- ✅ Файлы для разных хостингов:
  - `public/_redirects` - для Netlify
  - `vercel.json` - для Vercel
  - `public/.htaccess` - для Apache хостинга

## 📦 Сборка проекта

### 1. Установите зависимости (если еще не установлены):

```bash
npm install
```

### 2. Настройте переменные окружения для production:

Создайте файл `.env.production` в корне проекта:

```env
VITE_SUPABASE_URL=ваш_supabase_url
VITE_SUPABASE_ANON_KEY=ваш_supabase_anon_key
```

**Важно:** Эти переменные будут встроены в сборку при `npm run build`.

### 3. Соберите проект:

```bash
npm run build
```

После сборки в папке `dist/` будут готовые файлы для загрузки на хостинг.

### 4. Проверьте сборку локально:

```bash
npm run preview
```

Откройте http://localhost:4173 и проверьте, что все работает.

## 🚀 Деплой на разные хостинги

### Вариант 1: Netlify (Рекомендуется - бесплатно)

#### Способ A: Через Netlify CLI

1. Установите Netlify CLI:
```bash
npm install -g netlify-cli
```

2. Войдите в Netlify:
```bash
netlify login
```

3. Деплой:
```bash
npm run build
netlify deploy --prod --dir=dist
```

#### Способ B: Через веб-интерфейс

1. Зайдите на https://app.netlify.com
2. Перетащите папку `dist/` в окно браузера
3. Или подключите GitHub репозиторий для автоматического деплоя

**Настройки в Netlify:**
- Build command: `npm run build`
- Publish directory: `dist`
- Environment variables: добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

**Файл `public/_redirects` уже настроен** для работы SPA роутинга.

---

### Вариант 2: Vercel (Рекомендуется - бесплатно)

#### Способ A: Через Vercel CLI

1. Установите Vercel CLI:
```bash
npm install -g vercel
```

2. Деплой:
```bash
npm run build
vercel --prod
```

#### Способ B: Через веб-интерфейс

1. Зайдите на https://vercel.com
2. Импортируйте проект из GitHub
3. Настройки:
   - Framework Preset: Vite
   - Build Command: `npm run build`
   - Output Directory: `dist`
   - Environment Variables: добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

**Файл `vercel.json` уже настроен** для работы SPA роутинга.

---

### Вариант 3: GitHub Pages

1. Установите gh-pages:
```bash
npm install --save-dev gh-pages
```

2. Добавьте в `package.json`:
```json
{
  "scripts": {
    "predeploy": "npm run build",
    "deploy": "gh-pages -d dist"
  },
  "homepage": "https://ваш-username.github.io/ваш-репозиторий"
}
```

3. Обновите `vite.config.ts`:
```typescript
export default defineConfig({
  base: '/ваш-репозиторий/',
  // ... остальные настройки
})
```

4. Деплой:
```bash
npm run deploy
```

---

### Вариант 4: Обычный хостинг (Apache/Nginx)

#### Для Apache:

1. Соберите проект:
```bash
npm run build
```

2. Загрузите содержимое папки `dist/` на хостинг через FTP/SFTP

3. **Файл `.htaccess` уже включен** в папку `public/`, он автоматически скопируется в `dist/`

4. Убедитесь, что на хостинге включен mod_rewrite

#### Для Nginx:

1. Соберите проект:
```bash
npm run build
```

2. Загрузите содержимое папки `dist/` на хостинг

3. Настройте Nginx конфигурацию:
```nginx
server {
    listen 80;
    server_name ваш-домен.com;
    root /path/to/dist;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # Кэширование статических файлов
    location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
}
```

---

### Вариант 5: Cloudflare Pages

1. Зайдите на https://pages.cloudflare.com
2. Подключите GitHub репозиторий
3. Настройки:
   - Build command: `npm run build`
   - Build output directory: `dist`
   - Environment variables: добавьте `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

---

## 🔐 Настройка переменных окружения на хостинге

**Важно:** На всех хостингах нужно добавить переменные окружения:

- `VITE_SUPABASE_URL` - URL вашего Supabase проекта
- `VITE_SUPABASE_ANON_KEY` - Anon key из Supabase

### Где добавить:

- **Netlify:** Site settings → Environment variables
- **Vercel:** Project settings → Environment Variables
- **GitHub Pages:** Не поддерживает env переменные (используйте `.env.production`)
- **Обычный хостинг:** Создайте `.env.production` файл перед сборкой

## ✅ Проверка после деплоя

После деплоя проверьте:

1. ✅ Главная страница открывается
2. ✅ Роутинг работает (попробуйте `/login`, `/register`)
3. ✅ Авторизация работает (проверьте подключение к Supabase)
4. ✅ Все стили загружаются
5. ✅ Иконки отображаются

## 🐛 Решение проблем

### Проблема: Белая страница после деплоя

**Решение:** Проверьте, что все пути к файлам правильные. Если деплоите в подпапку, обновите `base` в `vite.config.ts`.

### Проблема: 404 при переходе по роутам

**Решение:** Убедитесь, что файл `_redirects` (Netlify) или `vercel.json` (Vercel) настроен правильно. Для Apache проверьте `.htaccess`.

### Проблема: Переменные окружения не работают

**Решение:** 
- Убедитесь, что переменные добавлены на хостинге
- Пересоберите проект после добавления переменных
- Проверьте, что переменные начинаются с `VITE_`

### Проблема: Ошибки CORS

**Решение:** Настройте CORS в Supabase:
1. Зайдите в Supabase Dashboard
2. Settings → API
3. Добавьте ваш домен в "Allowed Origins"

## 📝 Чеклист перед деплоем

- [ ] Проект собран без ошибок (`npm run build`)
- [ ] Локальная сборка работает (`npm run preview`)
- [ ] Переменные окружения настроены
- [ ] Supabase CORS настроен для вашего домена
- [ ] Все файлы для хостинга на месте (`_redirects`, `vercel.json`, `.htaccess`)
- [ ] Тестирование на dev сервере прошло успешно

## 🎉 Готово!

После выполнения всех шагов ваш сайт будет доступен на выбранном хостинге!


