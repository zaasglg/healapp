# Контекст проекта: Дневник подопечного

## Описание веб-приложения

**Дневник подопечного** — централизованный цифровой журнал состояния и ухода за подопечным, где данные фиксируют:
- **Клиент** (родственник/опекун)
- **Специализированный персонал** (сиделки, медсёстры)
- **Сам подопечный** (если он использует приложение)

### Основной функционал:
- Хранение структурированных медицинских и уходовых показателей
- Карточка подопечного с личными данными, диагнозами и мобильностью
- Мультимедиа (фото, видео) - можно прикреплять к показателям или добавлять отдельно с описанием
- Голосовые заметки
- Графики динамики показателей
- Экспорт отчётов (PDF/CSV)
- Гибкая система доступа:
  - Регистрация по пригласительным ссылкам для организаций и сиделок
  - Приглашение сотрудников организациями (все сотрудники с одинаковыми правами)
  - Ссылки для клиентов на просмотр дневника (read-only доступ, без срока действия, без регистрации)
  - Постоянная ссылка для администратора (без регистрации)

### Цель проекта:
Дать полную, достоверную картину состояния подопечного и предупреждать о рисках.

---

## Цели модуля

1. **Надёжно фиксировать** всю значимую информацию по ежедневному уходу и состоянию подопечного
2. **Давать удобный инструмент** для мониторинга и быстрого принятия решений врачу/родственнику/сиделке
3. **Обеспечить прозрачность** истории ухода (audit trail) для споров и отчетности
4. **Предоставить экспортные отчёты** (PDF/CSV) пригодные для передачи врачу или в гос. системы

---

## Переход на Supabase (бэкенд планы)

- **Цель:** Перенести все текущие фронтенд-данные из `localStorage` в Supabase, обеспечить единый источник правды и строгую систему прав доступа.
- **Основные источники данных:** `local_users`, `local_employees`, `local_clients`, `organizations`, `patient_cards`, `diaries`, `diary_metrics`, `diary_metric_values`, `diary_employee_access`, `diary_client_links`, `caregiver_client_invite_tokens`, `organization_client_invite_tokens`, `organization_invite_tokens`, `admin_static_tokens`.
- **Нормализация идентификаторов:** использовать функцию `normalizeId` (перед миграцией) для устранения дублей и смешанных форматов (`id`, `user_id`, `organization_id`).
- **Ключевые роли и сценарии:**
  - Пансионаты (`organization_type = 'pension'`) — сотрудники видят все дневники автоматически.
  - Патронажные агентства (`organization_type = 'patronage_agency'`) — сотрудники получают доступ только после явного назначения через `diary_employee_access`.
  - Частные сиделки (`organization_type = 'caregiver'`) — работают напрямую с клиентами.
  - Клиенты — владельцы карточек и дневников, управляют доступом организаций/сиделок.
  - Сотрудники организаций — действуют от имени организации, но права зависят от `organization_type`.
- **Supabase задачи:** подготовить таблицы (`organizations`, `organization_employees`, `clients`, `patient_cards`, `diaries`, `diary_metrics`, `diary_metric_values`, `diary_employee_access`, `diary_client_links`, `invite_tokens` и специализированные токен-таблицы), настроить RLS-политики, перенести данные и заменить фронтенд-запросы на RPC/Edge Functions.
- **Auth:** email+пароль для организаций/частных сиделок; телефон+пароль/OTP для сотрудников и клиентов; админ — постоянные токены. Edge Function `accept_invite_token` обрабатывает приглашения и создаёт учётные записи.
- **Контроль после миграции:** чек-листы по структуре таблиц, соответствию данных, тестовые сценарии (регистрация, назначение доступов, работа дневника, админ-панель).

---

## Стек технологий

### Backend
- **Supabase** — BaaS (Backend as a Service) платформа, включает:
  - **PostgreSQL** база данных с автоматически генерируемым REST API
  - **Supabase Auth** — аутентификация и авторизация пользователей
  - **Supabase Storage** — хранение файлов (фото, видео, голосовые заметки)
  - **Row Level Security (RLS)** — безопасность данных на уровне строк
  - **Edge Functions** — серверные функции для обработки данных
  - **Realtime** — возможность подписки на изменения в реальном времени

### Frontend
- **React** — библиотека для построения пользовательского интерфейса
- **TypeScript** — типизированный JavaScript для надёжности кода
- **Vite** или **Next.js** — инструменты сборки и разработки
- **React Query / TanStack Query** — управление состоянием серверных данных
- **React Router** — маршрутизация (если используется Vite)
- **Zustand** или **Redux Toolkit** — управление глобальным состоянием клиента
- **React Hook Form** — управление формами
- **Zod** — валидация данных

### UI/UX
- **Tailwind CSS** — стилизация компонентов (основной инструмент)
- **Recharts** или **Chart.js** — построение графиков динамики показателей
- **React PDF** или **jsPDF** — генерация PDF отчётов
- **Papa Parse** — работа с CSV файлами

**Дизайн-система:**
- **Шрифты:**
  - Основной: **Fira Sans**
  - Второй: **Manrope**
- **Цветовая палитра:**
  - Белый (#FFFFFF)
  - Темно-серый (#4A4A4A)
  - Голубой (#55ACBF)
  - Дополнительные цвета и их вариации берутся из дизайна
- **Дизайн:** Мобильная версия (дизайн в папке `Дизайн приложения/`)
  - Все экраны должны соответствовать предоставленному дизайну
  - Десктопная версия - адаптация мобильного дизайна
- **Иконки:** Использовать иконки из папки `public/icons/` (не использовать библиотеки иконок)

### Дополнительные инструменты
- **Date-fns** или **Day.js** — работа с датами
- **React Dropzone** — загрузка файлов
- **Web Audio API** — обработка голосовых заметок

---

## Архитектурные решения

### Система регистрации по пригласительным ссылкам

**Процесс регистрации:**
1. Администратор создаёт одноразовую пригласительную ссылку с уникальным токеном
2. Ссылка отправляется пансионату/патронажному агентству/сиделке
3. При переходе по ссылке открывается страница регистрации
4. Пользователь выбирает тип организации:
   - **Пансионат**
   - **Патронажное агентство**
   - **Сиделка**
5. Регистрация:
   - **Пансионат/Патронажное агентство/Сиделка**: регистрация по email и паролю (через Supabase Auth)
   - **Сотрудники организаций**: регистрация по номеру телефона и паролю (без email, без кода подтверждения телефона)
   - **Без токена**: на странице `/register` доступна открытая регистрация организаций/сиделок по email; при наличии `org_token` страница автоматически переключается в сценарий сотрудника (телефон + пароль) и помечает токен использованным
6. После регистрации пользователь попадает в личный кабинет
7. В личном кабинете заполняется профиль:
   - **Частная сиделка**: имя, фамилия, **номер телефона (обязательно)** — с подписью что это для связи с поддержкой в WhatsApp, **город (обязательно)**
   - **Пансионат/Патронажное агентство**: название организации, **номер телефона (обязательно)** — с подписью что это для связи с поддержкой в WhatsApp, **адрес (обязательно)** — место нахождения организации
   - **Сотрудники организаций**: только имя и фамилия (телефон уже известен из регистрации)
8. После заполнения профиля можно создавать дневники для подопечных
9. Организации и частные сиделки могут создавать ссылки для клиентов на просмотр дневников
10. Вход в систему:
    - **Организации и сиделки**: вход по email и паролю
    - **Сотрудники организаций**: вход по номеру телефона и паролю
- Административная панель доступна по маршруту `/admin` только при наличии валидного токена (`token` в URL). Допустимые токены задаются через переменные окружения (`VITE_ADMIN_TOKENS` или `VITE_ADMIN_TOKEN`). При успешной проверке токен кэшируется в `localStorage`, при ошибке пользователь перенаправляется на главную.
- Страница `/admin/invites` предоставляет интерфейс для генерации временных токенов (пока записываются в localStorage) и просмотра объединённого списка приглашений из Supabase (мок) и локальных источников; реализованы фильтры, поиск, карточки статистики (общее количество, активные/использованные, разбивка по типам) и копирование ссылки в буфер обмена. Поддерживаются приглашения для организаций, сотрудников, клиентов и частных сиделок.
- Страница `/admin/users` отображает сводную таблицу пользователей (организации, сотрудники, частные сиделки, клиенты), использует временные данные из локальных хранилищ, поддерживает поиск и фильтры по типам, подсчитывает количество дневников по связям.
- Страница `/admin/monitoring` выводит сводные карточки по ключевым метрикам, два столбчатых графика активности за последние 7 дней («Новые дневники» и «Активные дневники») с пояснениями и журнал действий поддержки; блок «Проблемные ситуации» удалён для более чистого экрана.
- Навигация админ-панели адаптирована под мобильные устройства: при узком экране основное меню свернуто в кнопку-гамбургер и раскрывается списком разделов с крупными touch-friendly элементами.
- Таблицы административных разделов (приглашения, пользователи, связи) автоматически переключаются на карточный формат на узких экранах, обеспечивая читабельность и скролл без горизонтального overflow; модальные окна занимают всю ширину на мобильных и фиксируют высоту через `100dvh`.

**Структура БД для пригласительных ссылок:**
- Таблица `invite_tokens`:
  - `id` (UUID, primary key)
  - `token` (string, unique) — уникальный токен для ссылки
  - `created_by` (UUID, FK к auth.users) — админ, создавший ссылку
  - `created_at` (timestamptz)
  - `expires_at` (timestamptz, nullable) — срок действия ссылки
  - `used_at` (timestamptz, nullable) — когда была использована
  - `used_by` (UUID, nullable, FK к auth.users) — кто использовал

**Структура БД для профилей:**
- Таблица `organizations`:
  - `id` (UUID, primary key)
  - `user_id` (UUID, FK к auth.users, unique)
  - `type` (enum: 'pension' | 'patronage_agency' | 'caregiver') — тип организации
  - `name` (text) — название организации (для пансионата/агентства) или ФИО (для сиделки)
  - `first_name` (text, nullable) — имя (только для сиделки)
  - `last_name` (text, nullable) — фамилия (только для сиделки)
  - `phone` (text, NOT NULL) — телефон (обязательное поле для всех типов для связи в WhatsApp)
  - `city` (text, NOT NULL для type='caregiver') — город (обязательное поле для частных сиделок)
  - `address` (text, NOT NULL для type='pension' | 'patronage_agency', nullable для type='caregiver') — адрес (обязательное поле для организаций - место нахождения)
  - `created_at` (timestamptz)
  - `updated_at` (timestamptz)

**Важные особенности для профилей:**
- Телефон является **обязательным полем** для всех типов пользователей
- При вводе телефона отображается подпись: *"Номер телефона нужен для связи с поддержкой в WhatsApp"*
- Телефон **не проверяется кодом** — просто вводится и сохраняется
- Телефон используется для связи службы поддержки через WhatsApp
- Для частных сиделок: **город является обязательным полем**
- Для организаций: **адрес является обязательным полем** (место нахождения организации)

**Важные особенности:**
- Каждая ссылка одноразовая (после использования помечается как `used`)
- Токен проверяется при регистрации
- Тип организации фиксируется при регистрации и не может быть изменён
- Профиль должен быть заполнен перед созданием дневников

### Система приглашений сотрудников организациями

**Процесс приглашения сотрудников:**
1. Организация (пансионат/патронажное агентство) создаёт пригласительную ссылку для сотрудника в своём личном кабинете
2. Ссылка отправляется сотруднику
3. Сотрудник переходит по ссылке и регистрируется по номеру телефона и паролю (без email, без кода подтверждения телефона)
4. При регистрации автоматически привязывается к организации
5. Сотрудник заполняет свой профиль (только имя и фамилия, телефон уже известен из регистрации)
6. Сотрудник получает полный доступ к дневникам организации (создание, редактирование, просмотр)
7. Вход в систему для сотрудников осуществляется по номеру телефона и паролю

**Структура БД для приглашений сотрудников:**
- Таблица `organization_invite_tokens`:
  - `id` (UUID, primary key)
  - `token` (string, unique) — уникальный токен для ссылки
  - `organization_id` (UUID, FK к organizations) — организация, создавшая ссылку
  - `organization_type` (enum) — фиксируется на момент генерации, чтобы в момент регистрации понять модель доступа (пансионат vs патронажное агентство)
  - `created_by` (UUID, FK к auth.users) — кто создал ссылку (представитель организации)
  - `created_at` (timestamptz)
  - `expires_at` (timestamptz, nullable) — срок действия ссылки (опционально)
  - `used_at` (timestamptz, nullable) — когда была использована
  - `used_by` (UUID, nullable, FK к auth.users) — кто использовал

- Таблица `organization_employees`:
  - `id` (UUID, primary key)
  - `user_id` (UUID, FK к auth.users, unique) — пользователь-сотрудник
  - `organization_id` (UUID, FK к organizations) — организация
  - `first_name` (text)
  - `last_name` (text)
  - `phone` (text, nullable)
  - `created_at` (timestamptz)
  - `updated_at` (timestamptz)

**Важные особенности:**
- Только организации (пансионаты/патронажные агентства) могут создавать ссылки для сотрудников
- Частные сиделки НЕ могут приглашать сотрудников (они работают сами)
- Ссылки одноразовые
- Сотрудник может быть привязан только к одной организации
- **Права доступа сотрудников зависят от роли:**
  - **Администратор/Врач/Руководитель**: могут создавать карточки подопечных и дневники, полный доступ
  - **Сиделка**: может только заполнять и просматривать дневники (не может создавать карточки и дневники)
- Для патронажных агентств после регистрации сотрудника доступ к конкретным дневникам выдаётся вручную через `diary_employee_access`, поэтому Edge Function принятия приглашения должна возвращать `organization_type` и не создавать автоматических записей доступа.

### Система приглашений клиентов

**Процесс приглашения клиентов:**

**Для частных сиделок:**
1. В профиле сиделки есть кнопка "Пригласить клиента"
2. Сиделка создаёт пригласительную ссылку для клиента (в модальном окне или на отдельной странице)
3. Ссылка отправляется клиенту
4. Клиент переходит по ссылке и регистрируется по номеру телефона и паролю (без email, без кода подтверждения телефона)
5. При регистрации автоматически привязывается к сиделке
6. Клиент заполняет свой профиль (имя, фамилия, телефон уже известен из регистрации)
7. Клиент создаёт карточку подопечного (родственника)
8. Клиент выбирает показатели для дневника и настраивает дневник
9. Создаётся дневник, доступный и сиделке, и клиенту
10. Сиделка заполняет дневник, клиент может просматривать и вносить изменения

**Для организаций:**
1. Администратор/врач/руководитель организации создаёт карточку подопечного (от имени клиента)
2. Администратор/врач/руководитель создаёт дневник на основе карточки
3. После создания карточки подопечного и дневника динамики организация может пригласить клиента (приглашение привязано к конкретной карточке и дневнику)
4. Приглашение создаётся:
   - После создания дневника в настройках дневника (вкладка "Доступ для клиента") - основной способ
   - В настройках карточки подопечного (кнопка "Пригласить клиента") - опционально
5. Ссылка отправляется клиенту
6. Клиент переходит по ссылке и регистрируется по номеру телефона и паролю
7. При регистрации автоматически привязывается к организации, к карточке подопечного и дневнику
8. Клиент заполняет свой профиль (имя, фамилия, телефон уже известен из регистрации)
9. После создания дневника ссылка на приглашение клиента всегда доступна в дневнике (вкладка "Доступ для клиента")
10. Клиент может просматривать дневник и карточку подопечного, управлять доступом

**Структура БД для приглашений клиентов:**
- Таблица `caregiver_client_invite_tokens` (для сиделок):
  - `id` (UUID, primary key)
  - `token` (string, unique) — уникальный токен для ссылки
  - `caregiver_id` (UUID, FK к organizations, где type='caregiver') — сиделка, создавшая ссылку
  - `created_by` (UUID, FK к auth.users) — кто создал ссылку (сиделка)
  - `created_at` (timestamptz)
  - `expires_at` (timestamptz, nullable) — срок действия ссылки (опционально)
  - `used_at` (timestamptz, nullable) — когда была использована
  - `used_by` (UUID, nullable, FK к auth.users) — кто использовал

- Таблица `organization_client_invite_tokens` (для организаций):
  - `id` (UUID, primary key)
  - `token` (string, unique) — уникальный токен для ссылки
  - `organization_id` (UUID, FK к organizations) — организация, создавшая ссылку
  - `patient_card_id` (UUID, NOT NULL, FK к patient_cards) — карточка подопечного, на которую приглашается клиент (обязательно!)
  - `diary_id` (UUID, nullable, FK к diaries) — дневник, к которому привязана ссылка (если дневник уже создан)
  - `created_by` (UUID, FK к auth.users) — кто создал ссылку (представитель организации)
  - `created_at` (timestamptz)
  - `expires_at` (timestamptz, nullable) — срок действия ссылки (опционально)
  - `used_at` (timestamptz, nullable) — когда была использована
  - `used_by` (UUID, nullable, FK к auth.users) — кто использовал

- Таблица `clients`:
  - `id` (UUID, primary key)
  - `user_id` (UUID, FK к auth.users, unique) — пользователь-клиент
  - `caregiver_id` (UUID, nullable, FK к organizations, где type='caregiver') — сиделка (если клиент приглашен сиделкой)
  - `organization_id` (UUID, nullable, FK к organizations) — организация (если клиент приглашен организацией)
  - `first_name` (text)
  - `last_name` (text)
  - `phone` (text, NOT NULL)
  - `created_at` (timestamptz)
  - `updated_at` (timestamptz)

**Важные особенности:**
- И сиделки, и организации могут приглашать клиентов
- Клиент может быть привязан либо к сиделке, либо к организации (не к обоим одновременно)
- Ссылки одноразовые
- Клиент регистрируется по телефону (как сотрудники)
- Клиент имеет профиль как у сиделки (имя, фамилия, телефон)

### Система создания дневников

**ВАЖНО: Карточка подопечного и дневник подопечного - это все клиентское и должно оставаться у клиента. Клиент управляет доступом.**

**Логика создания дневников:**

**Для частных сиделок:**
1. Сиделка приглашает клиента (создаёт пригласительную ссылку)
2. Клиент регистрируется и заполняет карточку подопечного
3. Клиент выбирает показатели для дневника
4. Создаётся дневник:
   - `owner_id` = `client_id` (клиент - владелец!)
   - `client_id` = `user_id` клиента
   - `patient_card_id` = ID карточки подопечного
   - `caregiver_id` = `user_id` сиделки (для доступа сиделки)
5. Доступ:
   - Клиент: полный доступ (владелец), может редактировать карточку, заполнять и просматривать дневник, управлять доступом сиделки
   - Сиделка: заполнение и просмотр дневника, просмотр карточки (доступ может быть отозван клиентом)
6. Клиент может убрать сиделку - она теряет доступ к дневнику

**Для организаций:**
1. Администратор/врач/руководитель создаёт карточку подопечного (от имени клиента)
2. После создания карточки организация приглашает клиента на эту карточку (приглашение привязано к `patient_card_id`)
3. Клиент регистрируется по ссылке и автоматически привязывается к карточке
4. Администратор/врач/руководитель создаёт дневник:
   - `owner_id` = `client_id` (клиент - владелец!)
   - `client_id` = `user_id` клиента
   - `patient_card_id` = ID карточки подопечного
   - `organization_id` = ID организации (для доступа организации)
   - `organization_type` фиксируется на момент создания (пансионат/патронажное агентство) и используется бэкендом для выбора модели доступа сотрудников
5. После создания дневника ссылка на приглашение клиента всегда доступна в дневнике (вкладка "Доступ для клиента")
6. Если клиент еще не зарегистрирован, ссылка позволяет ему зарегистрироваться и получить доступ
7. Если клиент уже зарегистрирован, ссылка позволяет ему получить доступ к дневнику
8. Доступ:
   - Клиент: полный доступ (владелец), может редактировать карточку, просматривать дневник, управлять доступом организации
   - Администратор/врач/руководитель: полный доступ (доступ может быть отозван клиентом)
   - Сиделки: только заполнение и просмотр дневника (не карточки, доступ может быть отозван клиентом)
9. Клиент может убрать организацию - она теряет доступ к дневнику

**Важно:**
- Без карточки подопечного невозможно создать дневник
- Карточка подопечного должна быть создана ДО создания дневника
- У одного подопечного только одна карточка может быть
- **У одного подопечного только один дневник может быть** - карточки, для которых уже создан дневник, не отображаются в списке при создании нового дневника
- Дневник всегда привязан к карточке подопечного через `patient_card_id`
- **Клиент - владелец карточки и дневника** - они остаются у него даже если он перестанет пользоваться услугами
- Клиент управляет доступом - может отозвать доступ сиделки/организации
- Если клиент перестанет пользоваться услугами, дневник и карточка остаются только у него

### Административная панель (Admin Panel)

**Концепция:**
Веб-админ-панель для управления системой, создания пригласительных ссылок и помощи зарегистрированным пользователям.

**Расположение:**
- Админ-панель является частью основного веб-сайта (роут `/admin`)
- Доступ через постоянную ссылку (без регистрации)
- Администратор не регистрируется в системе, у него есть постоянная ссылка для прямого доступа к админ-панели
- Ссылка содержит специальный токен для доступа
- Проверка доступа через валидацию токена в ссылке

**Функционал админ-панели:**

1. **Управление пригласительными ссылками:**
   - Создание новых пригласительных ссылок с уникальными токенами
   - Просмотр всех созданных ссылок (активные, использованные, истёкшие)
   - Копирование ссылки в буфер обмена
   - Установка срока действия ссылки (опционально)
   - Отметка ссылок как неактивных (если нужно отозвать)
   - Статистика по ссылкам (сколько использовано, сколько активных)

2. **Управление зарегистрированными пользователями:**
   - Просмотр списка всех организаций и сиделок
   - Фильтрация по типу (пансионат/патронажное агентство/сиделка)
   - Поиск по названию организации, имени, email
   - Просмотр профилей пользователей
   - Просмотр сотрудников организаций
   - Редактирование данных пользователей (при необходимости помочь)
   - Просмотр созданных дневников пользователя
   - Статистика активности пользователей

3. **Помощь и поддержка пользователей:**
   - Просмотр дневников подопечных (для помощи в заполнении)
   - Возможность редактировать/исправлять данные от имени пользователя
   - Просмотр истории изменений (audit trail)
   - Экспорт данных для технической поддержки
   - Сброс пароля пользователя (при необходимости)

4. **Мониторинг системы:**
   - Общая статистика (количество пользователей, дневников, записей)
   - Активность за период (регистрации, создание дневников)
   - Проблемные ситуации (незаполненные профили, неактивные пользователи)

**Технические детали:**

- **Доступ к админ-панели:**
  - Постоянная ссылка вида: `/admin?token=ADMIN_SECRET_TOKEN`
  - Токен проверяется при загрузке страницы
  - Токен хранится в переменных окружения
  - При неверном токене - редирект на главную страницу
  - RLS политики на бэкенде для защиты данных

- **Безопасность:**
  - Все действия админа логируются в таблицу `admin_actions` (audit trail)
  - Критические операции (сброс пароля, удаление данных) требуют подтверждения
  - Админ может видеть все данные, но RLS политики остаются для защиты от SQL-инъекций

- **Интерфейс:**
  - Современный UI с удобной навигацией
  - Таблицы с сортировкой и фильтрацией
  - Модальные окна для создания ссылок и редактирования данных
  - Уведомления об успешных операциях

**Структура БД для админ-панели:**
- Таблица `user_roles`:
  - `user_id` (UUID, FK к auth.users, primary key)
  - `role` (enum: 'admin' | 'organization' | 'private_caregiver' | 'org_employee')
  - `created_at` (timestamptz)
  
  *Примечание: клиенты не имеют аккаунтов в системе, они получают доступ через токены просмотра*

- Таблица `admin_actions` (audit log):
  - `id` (UUID, primary key)
  - `admin_id` (UUID, FK к auth.users)
  - `action_type` (text) — тип действия (create_invite, edit_user, etc.)
  - `target_user_id` (UUID, nullable) — на кого направлено действие
  - `details` (jsonb) — детали действия
  - `created_at` (timestamptz)

**Важно:**
- Админ-панель работает через тот же Supabase API, что и основное приложение
- Администратор не имеет аккаунта в системе, доступ только через токен в ссылке
- Все операции проходят через RLS (специальные политики для админа)
- Админ-панель доступна только по постоянной ссылке с токеном

### Аутентификация и роли
- **Основные таблицы:**
  - `auth.users` — базовые учётные записи Supabase.
  - `user_profiles` (проектируется) — расширенная информация о пользователе: тип роли, телефон в формате E.164, ссылка на организацию/клиента, технические флаги.
  - `organizations`, `organization_employees`, `clients` — профильные сущности домена.
- **Типы пользователей и способы входа:**
  1. **Администратор** — не регистрируется; доступ по постоянным токенам `admin_static_tokens`, проверка в Edge Function/route guard.
  2. **Организация (`organization_type = pension | patronage_agency`)** — регистрируется по email + пароль; профиль сохраняется в `organizations`; обязательны телефон и адрес (для агентства/пансионата).
  3. **Частная сиделка (`organization_type = caregiver`)** — регистрируется по email + пароль; в профиле фиксируются телефон и город; приглашают клиентов напрямую.
  4. **Сотрудник организации** — регистрируется по приглашению (`organization_invite_tokens`) с использованием телефона + пароль (план: Supabase phone OTP или сервисная функция с технич. email). В таблице `organization_employees` хранится роль (`admin`, `manager`, `caregiver`, `doctor`).
  5. **Клиент** — регистрируется по приглашению организации или частной сиделки, использует телефон + пароль; запись создаётся в `clients`, клиент обязательно становится владельцем карточки и дневника.
- **Особенности прав:**
  - Пансионат: все активные сотрудники автоматически видят дневники организации.
  - Патронажное агентство: сотрудники получают доступ только после явного назначения в `diary_employee_access`.
  - Частная сиделка: доступ только к своим клиентам/дневникам (`caregiver_id` в `diaries`).
  - Клиент управляет доступом к дневнику (может отозвать организацию, сиделку, сотрудника агентства) и остаётся владельцем карточки/дневника.
  - Администратор имеет полный доступ через сервисную роль и audit лог.
- **Сессии и metadata:** при регистрации по приглашению Edge Function `accept_invite_token` создаёт пользователя, заполняет `user_profiles`, помечает токен и возвращает JWT для фронта. Фронтенд должен прекратить использовать `localStorage` для хранения учетных записей и опираться на Supabase Auth.

### База данных
- **Row Level Security (RLS)** для контроля доступа к данным
- **Audit trail** — логирование всех изменений для прозрачности
- Структурированные таблицы для медицинских показателей
- Таблицы для связей между пользователями и подопечными

**Основные таблицы:**
- `invite_tokens` — пригласительные ссылки от администратора
- `organizations` — профили организаций и частных сиделок
- `organization_invite_tokens` — пригласительные ссылки для сотрудников
- `organization_employees` — сотрудники организаций
- `caregiver_client_invite_tokens` — пригласительные ссылки для клиентов от сиделок
- `organization_client_invite_tokens` — пригласительные ссылки для клиентов от организаций (привязаны к `patient_card_id`, создаются после создания карточки подопечного)
- `clients` — профили клиентов
- `patient_cards` — карточки подопечных (отдельная сущность, не привязана к дневнику)
- `diaries` — дневники подопечных (привязаны к карточке подопечного через `patient_card_id`)
- `diary_entries` — записи в дневнике (текстовые записи, заметки)
- `diary_metrics` — медицинские показатели (индивидуальные наборы для каждого подопечного)
- `diary_metric_values` — значения показателей за конкретную дату/время
- `diary_media` — медиафайлы (привязанные к показателям или отдельные)
- `user_roles` — роли пользователей
- `admin_actions` — логи действий администратора
- `diary_templates` — шаблоны записей для частых сценариев
- `medications` — справочник лекарств (запоминаются для каждого подопечного)
- `vitamins` — справочник витаминов (запоминаются для каждого подопечного)

**Структура таблицы `patient_cards`:**
- `id` (UUID, primary key)
- `client_id` (UUID, NOT NULL, FK к clients) — клиент-владелец карточки (обязательно!)
- `created_by` (UUID, FK к auth.users) — кто создал карточку (может быть клиент или администратор/врач/руководитель организации)
- `full_name` (text, NOT NULL) — ФИО подопечного
- `date_of_birth` (date, nullable) — дата рождения
- `height` (integer, nullable) — рост (см)
- `weight` (integer, nullable) — вес (кг)
- `gender` (enum: 'male' | 'female', NOT NULL) — пол
- `diagnoses` (jsonb) — массив диагнозов
- `mobility` (enum: 'walks' | 'sits' | 'lies', NOT NULL) — мобильность
- `created_at` (timestamptz)
- `updated_at` (timestamptz)

**Важно:**
- У одного подопечного только одна карточка может быть
- Клиент - владелец карточки (`client_id` обязателен)
- Карточка остается у клиента даже если он перестанет пользоваться услугами

**Структура таблицы `diaries` (обновленная):**
- `id` (UUID, primary key)
- `owner_id` (UUID, NOT NULL, FK к clients) — владелец дневника (клиент - обязательно!)
- `client_id` (UUID, NOT NULL, FK к clients) — клиент-владелец (дублирование для удобства запросов)
- `patient_card_id` (UUID, NOT NULL, FK к patient_cards) — карточка подопечного (обязательно!)
- `caregiver_id` (UUID, nullable, FK к organizations, где type='caregiver') — сиделка с доступом (может быть отозван клиентом)
- `organization_id` (UUID, nullable, FK к organizations) — организация с доступом (может быть отозван клиентом)
- `created_by` (UUID, FK к auth.users) — кто создал дневник (может быть клиент или администратор/врач/руководитель)
- `created_at` (timestamptz)
- `updated_at` (timestamptz)

**Важно:**
- Клиент - владелец дневника (`owner_id` = `client_id`)
- Дневник остается у клиента даже если он перестанет пользоваться услугами
- Клиент может отозвать доступ сиделки/организации (установить `caregiver_id` или `organization_id` в NULL)
- Без карточки подопечного невозможно создать дневник

### Хранение файлов
- **Supabase Storage** для мультимедиа:
  - Фото и видео — отдельные бакеты
  - Голосовые заметки — отдельный бакет
  - Политики доступа на уровне бакетов
- **Медиафайлы могут быть:**
  - Прикреплены к конкретному показателю (при заполнении)
  - Добавлены отдельно с описанием (не привязаны к показателю)
- **В истории показателей:** медиафайлы не отображаются, только указание "📎 Прикреплен медиафайл" и описание (если есть)

### Карточка подопечного

**Карточка подопечного — это отдельная сущность, не привязанная напрямую к дневнику.**

**Важно:**
- Карточка подопечного должна быть создана ДО создания дневника
- Без карточки подопечного невозможно создать дневник
- Один подопечный = одна карточка (но к карточке может быть привязано несколько дневников, если нужно)
- Карточку можно просматривать и редактировать из профиля пользователя

**Личные данные:**
- ФИО (обязательно)
- Дата рождения (опционально)
- Рост (опционально)
- Вес (опционально)
- Пол (обязательно)

**Диагнозы:**
- Выбор из списка: Перелом шейки бедра, Инфаркт, Альцгеймер, Инсульт, Паркинсон, Деменция
- Возможность добавить пользовательский диагноз
- Можно выбрать несколько диагнозов

**Мобильность:**
- Выбор одного варианта: ходит, сидит, лежит

**Кто может создавать карточки подопечных:**
- **Частные сиделки**: НЕ могут создавать карточки (создают клиенты)
  - Могут только просматривать карточки своих клиентов (где `caregiver_id` совпадает с ID сиделки)
  - Если нет карточек - показывается подсказка с кнопкой "Пригласить клиента"
- **Клиенты частных сиделок**: могут создавать карточки своих подопечных (они владельцы)
- **Организации**: администратор/врач/руководитель могут создавать карточки от имени клиента (но `client_id` обязателен - карточка принадлежит клиенту)
- **Сотрудники организаций (сиделки)**: НЕ могут создавать карточки
  - Могут только просматривать карточки организации (без возможности редактировать/удалять)
  - Если нет карточек - показывается сообщение "Нет карточек подопечных"
- **Клиенты организаций**: могут создавать карточки своих подопечных (они владельцы)

**Кто может редактировать карточки:**
- **Клиенты**: могут редактировать карточки своих подопечных (они владельцы)
- **Организации**: администратор/врач/руководитель могут редактировать карточки клиентов (если есть доступ)
- **Частные сиделки**: могут только просматривать карточки (НЕ могут редактировать или удалять, доступ может быть отозван клиентом)
- **Сотрудники организаций (сиделки)**: могут только просматривать карточки (НЕ могут редактировать или удалять, доступ может быть отозван клиентом)

**Управление доступом:**
- **Клиент** может отозвать доступ сиделки к дневнику (установить `caregiver_id` = NULL)
- **Клиент** может отозвать доступ организации к дневнику (установить `organization_id` = NULL)
- **Организация** может отозвать доступ у сотрудников или сиделок к дневнику
- После отзыва доступа сиделка/организация/сотрудник теряет возможность заполнять и просматривать дневник
- Карточка и дневник остаются у клиента навсегда
- **Интерфейс управления доступом:**
  - Раскрывающийся блок "Убрать дневник у специалиста" (красного цвета)
  - Отображение списка пользователей с доступом (с именами и ролями)
  - Кнопка удаления доступа для каждого пользователя
  - Раскрывающийся блок "Изменить закрепленные показатели" (голубого цвета)

### Структура медицинских показателей

**Индивидуальные наборы показателей:**
- Каждый подопечный имеет свой индивидуальный набор показателей
- При создании дневника предлагается выбрать показатели из общего пула
- Можно добавить свои показатели (пользователь прописывает название, и показатель добавляется в пул)
- Выбранные показатели сохраняются для конкретного подопечного
- **ВАЖНО:** В дневнике отображаются ТОЛЬКО те показатели, которые были выбраны при создании дневника
  - Если не выбраны закрепленные показатели - блок "Закрепленные показатели" не отображается
  - Если в разделе (Показатели ухода/Физические показатели/Тягостные симптомы) нет выбранных показателей - раздел не отображается
  - Внутри каждого раздела отображаются только выбранные пользователем метрики

**Блок 1: Параметры ухода**
1. **Прогулка** — выпадающее окно: время начала и окончания прогулки
2. **Когнитивные игры** — выпадающее окно: выбор из списка (шахматы, чтение, интересный разговор, настольные игры) или свой вариант
3. **Смена подгузников** — выпадающее окно: время смены
4. **Гигиена** — выпадающее окно: время проведения гигиены
5. **Увлажнение кожи** — выпадающее окно: время проведения
6. **Прием пищи** — выпадающее окно: время приема пищи
7. **Прием лекарств** — выпадающее окно: список лекарств, время приема. Лекарства запоминаются и предлагаются при следующем заполнении
8. **Прием витаминов** — выпадающее окно: список витаминов, время приема. Витамины запоминаются и предлагаются при следующем заполнении
9. **Сон** — выпадающее окно: время сна

**Блок 2: Физикальные параметры**
1. **Температура** — выпадающее окно: значение температуры, время замера
2. **Артериальное давление** — выпадающее окно: значение АД, время измерения
3. **Частота дыхания** — выпадающее окно: значение, время замера
4. **Уровень боли** — выпадающее окно: уровень от 0 до 10, время фиксации, комментарий (что болит)
5. **Сатурация** — выпадающее окно: значение, время измерения
6. **Уровень сахара в крови** — выпадающее окно: значение, время измерения

**Блок 3: Выделение мочи и кала**
1. **Выпито/выделено и цвет мочи** — выпадающее окно: количество выпитой воды (мл), количество выделенной мочи (мл), цвет мочи
2. **Дефекация** — выпадающее окно: было/не было

**Блок 4: Тягостные симптомы**
Все параметры: выпадающее окно с выбором "было/не было", при сохранении автоматически фиксируется примерное время
1. **Тошнота**
2. **Рвота**
3. **Одышка**
4. **Зуд**
5. **Кашель**
6. **Сухость во рту**
7. **Икота**
8. **Нарушение вкуса**

**Особенности:**
- Все показатели отображаются в виде карточек/кнопок
- При нажатии открывается модальное окно для заполнения (реализовано в интерфейсе)
  - Для булевых показателей (уход, симптомы, дефекация) — кнопки «Было» / «Не было» с подсветкой выбора и автофиксацией времени
  - Для числовых показателей (температура, сатурация, дыхание, сахар) — поле ввода с указанием единиц измерения
  - Для артериального давления — отдельные поля для САД/ДАД и опционально пульс, итоговое значение формируется автоматически
  - Для боли — шкала 0–10 (input range) + текстовое описание жалобы
  - Для блока «Выпито/выделено» — формы для объемов, выбор цвета мочи и свободный комментарий
  - Для когнитивных игр — пресеты активностей с возможностью добавить свой вариант + комментарий
  - Для сна/приёма пищи/лекарств/витаминов — текстовое описание без ручного ввода времени
- **Время заполнения автоматически фиксируется при сохранении** (текущее время)
- После сохранения данные привязываются к конкретной дате и времени
- Показатели можно заполнять несколько раз в день (для одного и того же параметра)
- История заполнения доступна через календарь (выбор дня)
- **К каждому показателю можно прикрепить фото/видео** при заполнения
- **Медиафайлы можно добавлять отдельно** (не привязанные к показателям) с описанием

**Вкладка «История»:**
- По умолчанию отображается текущий день, доступен выбор даты через календарь
- Записи выводятся группами по показателям, с указанием времени и значения
- Если за выбранную дату нет данных, показывается деликатное пустое состояние

**Вкладки доступа:**
- Для организаций доступна вкладка «Клиент» с подсказкой и генерацией персональной ссылки — можно скопировать или сразу отправить в WhatsApp; повторная генерация отзывает предыдущую ссылку. После перехода по ссылке дневник и карточка автоматически привязываются к аккаунту клиента, статус отображается в интерфейсе.
- Для клиентов доступна вкладка «Поделиться», позволяющая создавать несколько ссылок для сиделок/родственников, копировать их, делиться и отзывать доступ; активные ссылки отображаются списком с датой создания и кнопкой отправки в WhatsApp.
- История заполнения закрепляется в `diary_history` и доступна всем, кто имеет доступ к дневнику (клиент, сотрудники организации, частная сиделка)
- Обновлена система прав:
  - Клиент, получивший дневник по ссылке организации, становится владельцем и может отзывать доступ у организации и сиделки
  - Организация и её сотрудники пансионата продолжают видеть дневники и карточки, пока доступ не отозван
  - Сиделки патронажного агентства привязаны к конкретным дневникам и карточкам через `caregiver_id`
  - Частные сиделки имеют доступ к своим назначенным дневникам
- Мобильная версия оптимизирована: интерактивные элементы (кнопки, табы, карточки) имеют увеличенную touch-зону, а между вкладками дневника можно переключаться свайпами.
- Реализована регистрация клиента по приглашению:
  - Ссылки теперь ведут на `/client-invite`, где клиент проходит регистрацию, получает дневник и создаёт пароль
  - После регистрации клиент автоматически сохраняется в `local_users` и `local_clients`, дневник и карточка привязываются к пользователю
  - Во вкладке «Клиент» организация видит карточку клиента и ссылку для быстрого входа (аналогично сотрудникам)
  - Форма регистрации упрощена: убраны технический идентификатор дневника и дополнительная ссылка «Уже есть аккаунт?»
- Клиент, создающий новый дневник после регистрации, не предоставляет доступ организации автоматически — доступ остаётся только у клиента, пока он не поделится вручную или пока дневник не создан/активирован через приглашение
- Блок «Управление доступом»:
  - Патронажные агентства могут назначать/удалять своих сотрудников для заполнения дневника. По умолчанию сотрудники патронажного агентства не имеют доступа ни к одному дневнику — доступ выдаётся вручную для каждого дневника через этот блок.
  - Клиенты видят организации и частных специалистов с доступом и могут отзывать его
  - Пансионаты автоматически видят всех своих сотрудников с доступом и могут отключать каждого персонально
- Клиенты не видят кнопку «Отвязать карточку» в форме редактирования карточки; кнопка «Выйти из аккаунта» доступна только из экрана «Настройки», а не из главного профиля, для всех ролей
- Закреплённые показатели в карточках отображают только значение (без времени), а добавление сотрудников патронажным агентством фиксируется и отображается сразу (добавлены диагностические логи и fallback-загрузка сотрудников из localStorage)
- В профиле сотрудников организаций и частных сиделок скрыты элементы управления командой/клиентами и приглашением клиентов, а в дневнике для них не отображается блок управления доступом
- Кнопка «Настройки» в заголовке временно скрыта до финального UX-решения, сама модалка остаётся доступной для будущих задач

**Интерфейс заполнения закрепленных показателей:**
- **Модальное окно с анимацией:** красивая анимация открытия панели (slide-up), полноэкранная панель на мобильных
- **Блок заполнения параметра:**
  - Поле ввода значения показателя (зависит от типа: числовое, булево, текстовое)
  - Автоматическая фиксация времени заполнения
- **Блок выбора частоты заполнения:**
  - "Заполнять раз в": день, 2 раза в день, 3 раза в день, и т.д.
  - Возможность выбора произвольного количества раз в день
  - Сохранение настройки частоты для показателя
- **Блок напоминаний "С ДО":**
  - Выбор времени начала напоминаний (С)
  - Выбор времени окончания напоминаний (ДО)
  - Автоматический расчет промежутка времени между напоминаниями на основе частоты
  - Отображение расчетного времени напоминаний
- **Блок ИИ оценки помощника:**
  - Заголовок "Оценка ИИ помощника"
  - Текст: "Слишком мало данных для анализа, идет сбор"
  - Плейсхолдер для будущей интеграции с ИИ
- **Кнопка сохранения:** сохранение значения, настроек частоты и напоминаний, закрытие панели

### Просмотр истории заполнения показателей

**Интерфейс:**
- По умолчанию открыт сегодняшний день
- **Календарь для выбора даты** - можно выбрать любой день для просмотра истории
- Отображаются все заполненные показатели за выбранный день
- Каждый показатель показывает время заполнения и значения
- **Медиафайлы НЕ отображаются в истории** - вместо них текст "📎 Прикреплен медиафайл" (или аналогичный)
- Если у медиафайла есть описание, оно отображается рядом с указанием медиафайла
- Возможность просмотра истории изменений конкретного показателя
- При клике на указание медиафайла можно открыть просмотр (если нужно)

### Экспорт отчётов
- Генерация PDF на клиенте или через Edge Functions
- Экспорт CSV для интеграции с внешними системами
- Форматирование данных для врачей и гос. систем
- Кнопка "Поделиться в WhatsApp" для ссылки просмотра клиентом (прямое копирование ссылки)
- Кнопка "Копировать ссылку" в один клик для быстрого копирования

---

### Личный кабинет клиента

**Структура профиля клиента:**
- Личный кабинет доступен после регистрации и заполнения профиля (имя, фамилия, телефон)
- В личном кабинете отображаются только две кнопки:
  - **"Мои дневники"** — просмотр и управление дневниками подопечных
  - **"Карточки подопечных"** — просмотр и управление карточками подопечных
- Клиент НЕ может:
  - Приглашать сотрудников (кнопка "Сотрудники" скрыта)
  - Управлять другими клиентами (кнопка "Клиенты" скрыта)
  - Приглашать новых клиентов (кнопка "Пригласить клиента" скрыта)

**Редирект после регистрации:**
- После регистрации клиента по ссылке от сиделки или организации редирект на `/profile/setup`
- После заполнения профиля редирект на `/profile` (личный кабинет)
- В личном кабинете клиент может перейти в "Мои дневники" или "Карточки подопечных"

## Принципы UX/UI и производительности

### Автосохранение и надёжность
- **Автосохранение черновиков:** при вводе записей автоматическое сохранение черновиков каждые 30 секунд
- Защита от потери данных при сбоях браузера или сети
- Индикатор сохранения (автосохранено/сохранение...)

### Оптимистичные обновления
- UI обновляется сразу при действиях пользователя
- При ошибке — откат изменений и показ понятного сообщения
- Создаёт ощущение мгновенной отзывчивости

### Производительность
- **Ленивая загрузка:** записи подгружаются постранично (пагинация)
- **Lazy loading изображений:** изображения загружаются по мере прокрутки
- **Кэширование:** кэш последних записей для быстрого доступа
- **Офлайн-режим:** просмотр закэшированных данных без интернета

### Поиск и фильтрация
- Поиск по записям (текстовый поиск)
- Фильтры по датам (диапазон, конкретный день)
- Фильтры по типу записи
- Фильтр по автору (кто заполнил)
- Сохранение выбранных фильтров в сессии

### Индикаторы загрузки
- **Прогресс-бары** для загрузки файлов (фото, видео, аудио)
- **Скелетоны** (skeleton loaders) для контента при загрузке
- Плавные переходы между состояниями загрузки
- Индикатор синхронизации с сервером

### Обработка ошибок
- **Понятные сообщения:** вместо "Error 500" — "Не удалось сохранить запись. Попробуйте ещё раз."
- Человекочитаемые сообщения об ошибках на русском языке
- **Retry механизм:** автоматический повтор при сетевых ошибках (до 3 попыток)
- Показ кнопки "Повторить" при неудачных попытках

### Валидация форм
- **Zod схемы** на клиенте и сервере
- Валидация в реальном времени при вводе
- Подсказки при ошибках валидации прямо в полях
- Блокировка отправки формы с ошибками

### Mobile-first подход
- **Оптимизация для смартфонов** — приоритет мобильной версии
- **Touch-friendly:** большие кнопки, удобные тач-жесты
- Swipe-жесты для навигации (например, свайп для удаления записи)
- Адаптивные размеры элементов под размер экрана
- Вертикальная прокрутка как основной способ навигации

### Шаблоны записей
- Предзаполненные формы для частых сценариев
- Пользователь может создать свои шаблоны
- Быстрый выбор шаблона при создании записи
- Экономия времени при заполнении повторяющихся данных

### История изменений (Audit Trail в UI)
- Отображение истории изменений записей
- Информация: кто и когда редактировал
- Возможность просмотра предыдущих версий
- Визуальное выделение последних изменений

### Быстрые действия
- Кнопка "Копировать ссылку" в один клик (для ссылок клиентов)
- Кнопка "Поделиться в WhatsApp" для ссылки клиента (открывает WhatsApp с готовой ссылкой)
- Быстрое создание записи (кнопка на главной странице)
- Быстрое добавление фото/видео/аудио

## Примечания по разработке

- Все изменения в БД должны быть через миграции Supabase
- Использовать TypeScript для типизации
- Следовать принципам безопасности (RLS, валидация данных)
- Обеспечить доступность (a11y) интерфейса
- Мобильная адаптивность обязательна (mobile-first)
- Все пользовательские сообщения на русском языке
- Обработка всех возможных ошибок с понятными сообщениями

---

## Статус разработки

- [ ] Настройка проекта и Supabase
- [ ] Схема базы данных:
  - [ ] invite_tokens (пригласительные ссылки от админа)
  - [ ] organizations (профили организаций)
  - [ ] organization_invite_tokens (приглашения сотрудников)
  - [ ] organization_employees (сотрудники организаций)
  - [ ] diaries (дневники подопечных)
  - [ ] diary_view_tokens (ссылки для клиентов)
  - [ ] user_roles (роли пользователей)
  - [ ] admin_actions (логи админа)
- [ ] Система ролей и прав доступа (RLS политики)
- [ ] Система пригласительных ссылок (создание, валидация, одноразовость)
- [ ] Страница регистрации по ссылке с выбором типа организации
- [ ] Аутентификация и авторизация (Supabase Auth)
- [ ] Личный кабинет для заполнения профиля
- [ ] Личный кабинет организации:
  - [ ] Создание пригласительных ссылок для сотрудников
  - [ ] Управление сотрудниками (список сотрудников)
  - [ ] Создание ссылок для клиентов на просмотр дневников
- [ ] Личный кабинет частной сиделки:
  - [ ] Обязательное поле телефона с подписью про WhatsApp (без проверки кодом)
  - [ ] Создание ссылок для клиентов на просмотр дневников
- [ ] Административная панель:
  - [ ] Интерфейс создания пригласительных ссылок
  - [ ] Управление пользователями (просмотр, поиск, редактирование)
  - [ ] Просмотр дневников и помощь пользователям
  - [ ] Статистика и мониторинг
  - [ ] Audit log действий админа
- [ ] CRUD операции для дневника подопечного:
  - [ ] Создание/редактирование/удаление записей
  - [ ] Автосохранение черновиков (каждые 30 секунд)
  - [ ] Оптимистичные обновления UI
  - [ ] Валидация форм (Zod)
  - [ ] История изменений (отображение в UI)
- [ ] Система медицинских показателей:
  - [ ] Индивидуальные наборы показателей для каждого подопечного
  - [ ] Выбор показателей из пула при создании дневника
  - [ ] Добавление пользовательских показателей
  - [ ] Реализация всех 4 блоков показателей:
    - [ ] Блок 1: Параметры ухода (9 параметров)
    - [ ] Блок 2: Физикальные параметры (6 параметров)
    - [ ] Блок 3: Выделение мочи и кала (2 параметра)
    - [ ] Блок 4: Тягостные симптомы (8 параметров)
  - [ ] Запоминание лекарств и витаминов для каждого подопечного
  - [ ] Модальные окна для заполнения каждого показателя
- [ ] Просмотр истории заполнения показателей:
  - [ ] Отображение сегодняшнего дня по умолчанию
  - [ ] Календарь для выбора дня
  - [ ] Просмотр истории за выбранный день
  - [ ] Отображение времени заполнения и значений
- [ ] Система доступа сотрудников к дневникам организации (одинаковые права для всех)
- [ ] Страница просмотра дневника для клиентов (read-only по ссылке, без регистрации, без срока действия):
  - [ ] Кнопка "Копировать ссылку"
  - [ ] Кнопка "Поделиться в WhatsApp"
- [ ] Загрузка файлов (мультимедиа, голосовые заметки):
  - [ ] Прогресс-бары для загрузки
  - [ ] Lazy loading изображений
- [ ] Поиск и фильтрация:
  - [ ] Поиск по записям
  - [ ] Фильтры по датам, типу записи, автору
- [ ] Производительность:
  - [ ] Ленивая загрузка записей (пагинация)
  - [ ] Кэширование последних записей
  - [ ] Офлайн-режим для просмотра
  - [ ] Скелетоны при загрузке
- [ ] Обработка ошибок:
  - [ ] Понятные сообщения об ошибках
  - [ ] Retry механизм (автоматический повтор)
- [ ] Шаблоны записей:
  - [ ] Создание шаблонов
  - [ ] Использование шаблонов при создании записей
- [ ] Графики и визуализация данных
- [ ] Экспорт отчётов (PDF/CSV)
- [ ] Mobile-first оптимизация:
  - [ ] Touch-friendly интерфейс
  - [ ] Адаптивный дизайн
  - [ ] Swipe-жесты

- Клиент, зарегистрировавшийся по ссылке частной сиделки, автоматически привязывается к сиделке; созданные клиентом дневники получают `caregiver_id` сиделки и отображаются у неё в списке дневников.

