# Дневник подопечного

Веб-приложение для ведения дневника состояния и ухода за подопечными.

## 🚀 Быстрый старт

### Установка зависимостей

```bash
npm install
```

### Настройка переменных окружения

Создайте файл `.env.local` в корне проекта:

```env
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
```

### Запуск dev сервера

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

## 📦 Сборка для production

```bash
npm run build
```

Собранные файлы будут в папке `dist/`

### Предпросмотр production сборки

```bash
npm run preview
```

## 🌐 Деплой на хостинг

Проект полностью готов к деплою! Подробные инструкции смотрите в файле **[DEPLOY.md](./DEPLOY.md)**

### Поддерживаемые хостинги:

- ✅ **Netlify** (рекомендуется) - бесплатно, простой деплой
- ✅ **Vercel** (рекомендуется) - бесплатно, отличная производительность
- ✅ **GitHub Pages** - бесплатно для публичных репозиториев
- ✅ **Обычный хостинг** (Apache/Nginx) - через FTP/SFTP
- ✅ **Cloudflare Pages** - бесплатно, быстрый CDN

## 📁 Структура проекта

```
├── public/              # Статические файлы (иконки, _redirects, .htaccess)
├── src/
│   ├── components/      # React компоненты
│   │   ├── layout/      # Layout компоненты
│   │   └── ui/          # UI компоненты (Button, Input)
│   ├── pages/           # Страницы приложения
│   ├── hooks/           # Кастомные хуки
│   ├── store/           # Zustand хранилища
│   ├── lib/             # Утилиты и конфигурация
│   └── styles/          # Глобальные стили
├── .env.local           # Локальные переменные окружения
├── vite.config.ts       # Конфигурация Vite
└── package.json         # Зависимости проекта
```

## 🛠 Технологии

- **React 18** - UI библиотека
- **TypeScript** - типизация
- **Vite** - сборщик
- **React Router** - маршрутизация
- **Tailwind CSS** - стилизация
- **Supabase** - Backend as a Service
- **Zustand** - управление состоянием
- **React Hook Form** - работа с формами
- **Zod** - валидация

## 📚 Документация

- **[CONTEXT.md](./CONTEXT.md)** - полное описание проекта и архитектуры
- **[PLAN.md](./PLAN.md)** - детальный план разработки
- **[DEPLOY.md](./DEPLOY.md)** - инструкции по деплою
- **[SETUP_INSTRUCTIONS.md](./SETUP_INSTRUCTIONS.md)** - инструкции по настройке
- **[DESIGN_UPDATES.md](./DESIGN_UPDATES.md)** - описание дизайна

## 📝 Скрипты

- `npm run dev` - запуск dev сервера
- `npm run build` - сборка для production
- `npm run preview` - предпросмотр production сборки
- `npm run lint` - проверка кода линтером

## 🔐 Безопасность

- Переменные окружения не коммитятся в Git (см. `.gitignore`)
- Используйте `.env.local` для локальной разработки
- Для production создайте `.env.production` или настройте переменные на хостинге

## 📄 Лицензия

Private project


