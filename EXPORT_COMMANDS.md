# 📋 Команды для экспорта миграций из Supabase

## 🔍 Шаг 1: Где найти Project-Ref

### Способ 1: Из URL в браузере
1. Откройте https://supabase.com
2. Войдите: `nazardubnak@gmail.com` / `Dn2907200!`
3. Откройте проект **"Дневник подопечного"**
4. Посмотрите на URL в адресной строке браузера:
   ```
   https://supabase.com/dashboard/project/abcdefghijklmnop
   ```
   Где `abcdefghijklmnop` - это ваш **project-ref**

### Способ 2: Из Settings
1. В проекте перейдите в **Settings** (шестеренка внизу слева)
2. Откройте **General**
3. Найдите **Reference ID** - это ваш project-ref

### Способ 3: Через команду (после авторизации)
После выполнения `npx supabase login` выполните:
```powershell
npx supabase projects list
```
В списке будет project-ref для каждого проекта.

---

## 🚀 Шаг 2: Выполните команды по порядку

### Команда 1: Авторизация
```powershell
npx supabase login
```
- Нажмите **Enter**
- Откроется браузер
- Войдите: `nazardubnak@gmail.com` / `Dn2907200!`
- Подтвердите доступ

### Команда 2: Просмотр списка проектов
```powershell
npx supabase projects list
```
Покажет все ваши проекты с project-ref.

### Команда 3: Связывание с проектом
```powershell
npx supabase link --project-ref ВАШ_PROJECT_REF
```
**Замените `ВАШ_PROJECT_REF`** на реальный project-ref из шага 1.

**Пример:**
```powershell
npx supabase link --project-ref abcdefghijklmnop
```

### Команда 4: Создание полного дампа
```powershell
npx supabase db dump --schema public,auth,storage -f full_dump.sql
```

Это создаст файл `full_dump.sql` в текущей директории со всеми таблицами, функциями, триггерами и политиками.

---

## ✅ Проверка результата

После выполнения команды 4 проверьте:

```powershell
# Проверка размера файла
Get-Item full_dump.sql | Select-Object Name, Length

# Проверка содержимого (первые строки)
Get-Content full_dump.sql -Head 50
```

В файле должны быть строки:
- `CREATE TABLE public.user_profiles`
- `CREATE TABLE public.clients`
- `CREATE TABLE public.organizations`
- `CREATE TABLE public.diaries`
- И другие ваши таблицы

---

## 📤 Шаг 3: Отправка файла

После успешного экспорта:
1. Файл `full_dump.sql` будет в директории проекта
2. Пришлите этот файл мне
3. Я применю все миграции на новом сервере `176.124.217.224`

---

## 🔧 Если возникли ошибки

### Ошибка: "Not logged in"
```powershell
npx supabase login
```

### Ошибка: "Project not found"
- Проверьте правильность project-ref
- Убедитесь, что вы авторизованы

### Ошибка: "Permission denied"
- Убедитесь, что у вас есть доступ к проекту
- Проверьте права доступа в Supabase Dashboard

---

## 📝 Полный список команд (копируйте по порядку):

```powershell
# 1. Авторизация
npx supabase login

# 2. Список проектов (чтобы найти project-ref)
npx supabase projects list

# 3. Связывание (замените YOUR_PROJECT_REF)
npx supabase link --project-ref YOUR_PROJECT_REF

# 4. Экспорт дампа
npx supabase db dump --schema public,auth,storage -f full_dump.sql
```

**Готово!** Файл `full_dump.sql` будет содержать все миграции.

