FROM supabase/edge-runtime:v1.43.2

# Копируем Edge Functions в образ
# Edge Runtime ищет функции в /var/functions по умолчанию
COPY supabase/functions /var/functions

# Убеждаемся, что директория существует
RUN mkdir -p /var/functions

WORKDIR /var/functions

