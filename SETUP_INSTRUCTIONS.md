# Инструкции по настройке проекта

## ✅ Что уже сделано

### ЭТАП 1: Подготовка и настройка проекта
- ✅ Проект создан с React + TypeScript + Vite
- ✅ Структура папок настроена
- ✅ Все зависимости установлены:
  - react-router-dom
  - @tanstack/react-query
  - @supabase/supabase-js
  - react-hook-form
  - zod
  - zustand
  - tailwindcss
  - date-fns
- ✅ Настроены пути импорта (@/components, @/lib)
- ✅ Tailwind CSS настроен (конфиг, postcss, стили)
- ✅ Шрифты подключены (Fira Sans, Manrope)
- ✅ Цветовая палитра настроена
- ✅ Создан файл .env.local (требует заполнения credentials)

### ЭТАП 2: Базовые компоненты и Layout
- ✅ Layout компоненты созданы:
  - MainLayout (шапка + контент)
  - AuthLayout (для страниц авторизации)
  - Header (навигация и выход)
- ✅ UI компоненты созданы:
  - Button (с вариантами primary/secondary/outline)
  - Input (с валидацией и ошибками)

### ЭТАП 3: Система аутентификации (Frontend)
- ✅ Страница регистрации (RegisterPage):
  - Получение токена из URL
  - Выбор типа организации (Пансионат/Агентство/Сиделка)
  - Форма регистрации с валидацией (Zod)
  - Интеграция с Supabase Auth
- ✅ Страница входа (LoginPage):
  - Форма входа с валидацией
  - Интеграция с Supabase Auth
  - Обработка ошибок
- ✅ Система управления сессией:
  - Zustand store (authStore) для состояния авторизации
  - Хук useAuth для удобного использования
  - ProtectedRoute компонент для защиты роутов
  - Автоматическое обновление сессии

## 📋 Что нужно сделать для запуска

### 1. Создать проект в Supabase

1. Перейдите на https://supabase.com
2. Создайте новый проект
3. Дождитесь инициализации базы данных

### 2. Получить credentials

1. В панели Supabase перейдите в Settings → API
2. Найдите:
   - Project URL (например: https://xxxxx.supabase.co)
   - anon/public key (например: eyJhbGciOiJIUzI1NiIsInR5cCI...)

### 3. Добавить credentials в .env.local

Откройте файл `.env.local` в корне проекта и замените placeholder значения:

```env
VITE_SUPABASE_URL=ваш_project_url
VITE_SUPABASE_ANON_KEY=ваш_anon_key
```

### 4. Запустить dev сервер

```bash
npm run dev
```

Приложение будет доступно по адресу: http://localhost:5173

### 5. Настроить Supabase CLI и миграции

**Установка и логин**
```bash
npm install -g supabase
supabase --version
supabase login
```
> Логин откроет браузер и попросит подтвердить доступ к Supabase CLI.

**Связать локальный проект с Supabase**
```bash
supabase link --project-ref YOUR_PROJECT_REF
```
- `YOUR_PROJECT_REF` можно найти в панели Supabase: Settings → General → Project Reference.
- CLI создаст папку `.supabase` и сохранит настройки подключения (не коммитить).

**Переменные окружения для миграций**
Добавьте в `.env.local` (и при необходимости `.env`) следующие плейсхолдеры:
```env
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key
SUPABASE_DB_PASSWORD=пароль_БД_из_Settings→Database
```
Эти значения не используются на фронтенде, но пригодятся для утилиты миграции и Edge Functions. Храните их вне репозитория.

**Проверочный SQL (через Supabase SQL Editor)**
Выполните запрос, чтобы убедиться, что права записи работают:
```sql
create table if not exists migration_health_check (
  id uuid primary key default gen_random_uuid(),
  created_at timestamptz not null default now()
);
```
Если таблица создаётся успешно, инфраструктура готова. После проверки её можно удалить:
```sql
drop table if exists migration_health_check;
```

**Проверочный migration файл**
Можно создать пустую миграцию и применить её:
```bash
supabase migration new init_health_check
# отредактируйте файл migrations/.../init_health_check.sql при необходимости
supabase db push
```
Команда `supabase db push` должна пройти без ошибок — это подтвердит корректное подключение CLI и прав.

## Подготовка к миграции на Supabase

1. **Очистка локальных данных** — перед переключением на Supabase зафиксируйте и удалите локальные ключи. Подробно: `docs/local_storage_cleanup.md`.
2. **Seed Supabase демо-данными** — после чистой базы запустите сид-скрипт:
   ```bash
   SUPABASE_SERVICE_ROLE_KEY=... VITE_SUPABASE_URL=... npx ts-node scripts/seedSupabase.ts
   ```
   Скрипт создаст тестовую организацию, клиента, карточку и дневник.

## 🔗 Доступные роуты

- `/login` - Страница входа
- `/register?token=XXX` - Страница регистрации (требует токен приглашения)
- `/dashboard` - Главная страница (защищен авторизацией)
- `/profile/setup` - Настройка профиля после регистрации

## 📝 Следующие шаги разработки

После настройки Supabase и проверки авторизации:

1. **Создать таблицу в БД для пригласительных ссылок** (invite_tokens)
2. **Создать таблицу для профилей организаций** (organizations)
3. **Реализовать страницу настройки профиля** (ProfileSetupPage)
4. **Реализовать Dashboard** (DashboardPage)
5. **Создать функционал управления дневниками**

## ⚠️ Важные замечания

- Проект настроен для **mobile-first** дизайна
- Все сообщения на **русском языке**
- Используются иконки из папки `public/icons/` (не библиотеки иконок)
- UI компоненты создаются **по мере необходимости**
- Дизайн находится в папке `Дизайн приложения/`

## 🐛 Проверка ошибок

Для проверки линтера:
```bash
npm run lint
```

Для сборки проекта:
```bash
npm run build
```

## 📚 Документация

- **CONTEXT.md** - полное описание проекта и архитектуры
- **PLAN.md** - детальный план разработки с отметками выполненных задач
- Дизайн: `Дизайн приложения/`

## Edge Functions
1. Перед деплоем убедитесь, что в `.env.local` заполнены `SUPABASE_SERVICE_ROLE_KEY` и `VITE_SUPABASE_URL`.
2. Для локальной отладки: `supabase functions serve accept-invite --env-file .env.local`.
3. Для выкладки: `supabase functions deploy accept-invite` (будет использовать `SUPABASE_SERVICE_ROLE_KEY`).


