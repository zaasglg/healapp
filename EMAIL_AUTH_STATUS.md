# Статус аутентификации по email

## ✅ Что уже настроено:

1. **Регистрация по email включена:**
   - `GOTRUE_EXTERNAL_EMAIL_ENABLED=true` ✅
   - `GOTRUE_MAILER_AUTOCONFIRM=true` ✅

2. **Скрипты и инструкции созданы:**
   - `/root/configure_smtp_now.sh` - автоматическая настройка
   - `/root/SMTP_QUICK_SETUP.md` - подробная инструкция

## ❌ Что нужно сделать:

**SMTP сервер не настроен** - нужно добавить настройки в docker-compose.yml

---

## 🚀 Быстрая настройка (2 варианта):

### Вариант 1: Автоматический (рекомендуется)

Запустите скрипт на сервере - он запросит все данные:

```bash
ssh root@176.124.217.224
bash /root/configure_smtp_now.sh
```

Скрипт спросит:
- SMTP Host (например, `smtp.gmail.com`)
- SMTP Port (`587`)
- Ваш email
- Пароль приложения (для Gmail)
- Имя отправителя

После этого автоматически:
- ✅ Добавит настройки в docker-compose.yml
- ✅ Перезапустит контейнер auth
- ✅ Проверит работу

---

### Вариант 2: Ручная настройка

1. **Откройте docker-compose.yml:**
   ```bash
   ssh root@176.124.217.224
   nano /root/HealApp-Web/docker-compose.production.yml
   ```

2. **Найдите строку 51** (где `GOTRUE_MAILER_AUTOCONFIRM: "true"`)

3. **Добавьте после неё:**
   ```yaml
   GOTRUE_SMTP_HOST: smtp.gmail.com
   GOTRUE_SMTP_PORT: 587
   GOTRUE_SMTP_USER: ваш_email@gmail.com
   GOTRUE_SMTP_PASS: ваш_пароль_приложения
   GOTRUE_SMTP_ADMIN_EMAIL: ваш_email@gmail.com
   GOTRUE_SMTP_SENDER_NAME: Supabase
   GOTRUE_MAILER_AUTOCONFIRM: "false"  # Измените на false для тестирования
   ```

4. **Сохраните** (Ctrl+O, Enter, Ctrl+X)

5. **Перезапустите:**
   ```bash
   docker-compose -f /root/HealApp-Web/docker-compose.production.yml restart auth
   ```

---

## 📧 Для Gmail:

1. **Включите двухэтапную аутентификацию:**
   - https://myaccount.google.com/security
   - Включите "Двухэтапная аутентификация"

2. **Создайте пароль приложения:**
   - https://myaccount.google.com/apppasswords
   - Выберите "Почта" и "Другое устройство"
   - Введите имя: "Supabase"
   - Скопируйте 16-символьный пароль

3. **Используйте этот пароль** в `GOTRUE_SMTP_PASS`

---

## ✅ После настройки:

1. **Проверьте логи:**
   ```bash
   docker logs supabase-auth --tail 50 | grep -i smtp
   ```

2. **Протестируйте регистрацию:**
   - Откройте ваше приложение
   - Попробуйте зарегистрироваться по email
   - Проверьте почту (включая спам)

---

## 📝 Итого:

- ✅ Регистрация по email **включена**
- ❌ SMTP **не настроен** (нужны ваши данные)
- ✅ Скрипты **готовы** к использованию

**Следующий шаг:** Запустите `/root/configure_smtp_now.sh` или настройте вручную

