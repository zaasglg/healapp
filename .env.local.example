# Copy this file to .env.local and fill values before running the app
# Replace placeholders with values from your Supabase project and your chosen admin token(s)

# Public Supabase API (Project URL)
VITE_SUPABASE_URL="https://your-project-ref.supabase.co"

# Public (anon) key for client-side usage
VITE_SUPABASE_ANON_KEY="your-public-anon-key"

# Optional: admin tokens used to access /admin route. Comma-separated.
# Example: VITE_ADMIN_TOKENS="token1,token2"
VITE_ADMIN_TOKENS=""
# Optional single token variant
VITE_ADMIN_TOKEN=""

# Edge Functions / server usage: Service Role key (keep secret, do NOT commit to git)
# Used by server-side code / edge functions (SUPABASE_SERVICE_ROLE_KEY should be set in your server env)
SUPABASE_SERVICE_ROLE_KEY=""

# LOCAL DEVELOPMENT ONLY: Service Role key for admin panel direct access
# WARNING: Never use this in production! Only for local development when Edge Functions are unavailable
VITE_SUPABASE_SERVICE_ROLE_KEY=""

# Notes:
# - After creating .env.local you must restart the dev server for Vite to pick up VITE_* variables.
# - You can find Supabase values in the Supabase dashboard: Project Settings -> API -> Project URL and anon/public key.
# - Keep service role key private and only set it in your deployment environment, not in client .env files.
