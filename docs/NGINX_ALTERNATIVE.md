# Альтернативный способ: Использовать GitHub Actions

## 🎯 Идея

Вместо ввода команд вручную, можно использовать GitHub Actions для автоматического выполнения команд на сервере.

**НО:** Для этого нужен SSH доступ с GitHub Actions на сервер.

---

## ✅ План действий

1. **Настроить SSH доступ** (открыть порт 22, настроить SSH)
2. **Использовать GitHub Actions** для автоматического выполнения команд
3. **Все команды выполнятся автоматически** при push в репозиторий

---

## 🚀 Вариант: Настроить SSH через веб-консоль (короткие команды)

### Команда 1: Открыть порт 22
```bash
ufw allow 22/tcp
```

### Команда 2: Перезапустить SSH
```bash
systemctl restart ssh
```

### Команда 3: Включить доступ по паролю (если нужно)
```bash
sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config && systemctl restart ssh
```

---

## 📝 После настройки SSH

Я смогу подключиться к серверу и выполнить все команды автоматически!

---

## 🎯 Что выбрать?

**Вариант А:** Настроить SSH (3 короткие команды) → Я выполню всё автоматически
**Вариант Б:** Ввести команды для Nginx вручную (7 команд)

Какой вариант выбираешь?

