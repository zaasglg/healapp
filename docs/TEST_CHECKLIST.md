# Чек-лист тест-проверок интеграции с Supabase

**Дата:** 12 ноября 2025  
**Этап:** ЭТАП 16 - Бэкенд — серверная логика и интеграция

---

## ✅ Выполненные обновления

### Шаг 16.2: Управление приглашениями
- [x] `EmployeesPage` обновлен для использования RPC `generate_invite_link`
- [x] `EmployeesPage` обновлен для использования RPC `revoke_invite_link`
- [x] Загрузка сотрудников из Supabase (`organization_employees`)
- [x] Загрузка приглашений из Supabase (`invite_tokens` с `organization_invite_tokens`)

### Шаг 16.3: Дневники и доступы
- [x] `CreateDiaryPage` обновлен для использования RPC `create_diary`
- [x] `DashboardPage` уже использует Supabase для загрузки дневников
- [x] `DiaryPage` обновлен для использования RPC `assign_employee_to_diary`
- [x] `DiaryPage` обновлен для использования RPC `remove_employee_from_diary`
- [x] Загрузка метрик из Supabase (`diary_metrics`)
- [x] Загрузка назначенных сотрудников из Supabase (`diary_employee_access`)

### Шаг 16.4: Метрики и история
- [x] `DiaryPage` обновлен для использования RPC `save_metric_value`
- [x] `DiaryPage` обновлен для использования RPC `get_diary_history`
- [x] Загрузка значений метрик из Supabase (`diary_metric_values`)
- [x] Загрузка истории при изменении выбранной даты

---

## 🧪 Тест-проверки

### 1. Тест создания приглашения сотрудника
**Шаги:**
1. Войти как организация (пансионат или патронажное агентство)
2. Перейти в "Сотрудники" (`/profile/employees`)
3. Выбрать роль сотрудника
4. Нажать "Создать пригласительную ссылку"

**Ожидаемый результат:**
- ✅ Ссылка создается через RPC `generate_invite_link`
- ✅ Ссылка отображается в списке активных приглашений
- ✅ Ссылка автоматически копируется в буфер обмена
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM invite_tokens 
WHERE invite_type = 'organization_employee' 
ORDER BY created_at DESC LIMIT 1;
```

---

### 2. Тест отзыва приглашения
**Шаги:**
1. В списке активных приглашений нажать "Удалить"
2. Подтвердить удаление

**Ожидаемый результат:**
- ✅ Приглашение отзывается через RPC `revoke_invite_link`
- ✅ Приглашение исчезает из списка активных
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM invite_tokens 
WHERE id = '<invite_id>' AND revoked_at IS NOT NULL;
```

---

### 3. Тест загрузки сотрудников
**Шаги:**
1. Войти как организация
2. Перейти в "Сотрудники"
3. Проверить список сотрудников

**Ожидаемый результат:**
- ✅ Список сотрудников загружается из `organization_employees`
- ✅ Отображаются имя, фамилия, роль, телефон
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM organization_employees 
WHERE organization_id = '<org_id>';
```

---

### 4. Тест создания дневника
**Шаги:**
1. Войти как клиент или организация
2. Перейти в "Мои дневники" → "Создать новый дневник"
3. Выбрать карточку подопечного
4. Выбрать показатели (закрепленные и обычные)
5. Нажать "Создать дневник"

**Ожидаемый результат:**
- ✅ Дневник создается через RPC `create_diary`
- ✅ Метрики автоматически создаются в `diary_metrics`
- ✅ Редирект на страницу дневника
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
-- Проверка дневника
SELECT * FROM diaries WHERE id = '<diary_id>';

-- Проверка метрик
SELECT * FROM diary_metrics WHERE diary_id = '<diary_id>';
```

---

### 5. Тест загрузки дневников в Dashboard
**Шаги:**
1. Войти как любой пользователь с доступом к дневникам
2. Перейти в "Мои дневники" (`/dashboard`)

**Ожидаемый результат:**
- ✅ Список дневников загружается из Supabase
- ✅ Отображаются карточки подопечных
- ✅ Поиск работает корректно
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT d.*, pc.full_name, pc.date_of_birth 
FROM diaries d
LEFT JOIN patient_cards pc ON d.patient_card_id = pc.id
ORDER BY d.created_at DESC;
```

---

### 6. Тест сохранения метрики
**Шаги:**
1. Открыть дневник
2. Нажать на любой показатель
3. Заполнить значение
4. Сохранить

**Ожидаемый результат:**
- ✅ Значение сохраняется через RPC `save_metric_value`
- ✅ Значение появляется в истории
- ✅ Триггер автоматически добавляет запись в `diary_history`
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
-- Проверка значения метрики
SELECT * FROM diary_metric_values 
WHERE diary_id = '<diary_id>' 
ORDER BY recorded_at DESC LIMIT 1;

-- Проверка истории
SELECT * FROM diary_history 
WHERE diary_id = '<diary_id>' 
ORDER BY occurred_at DESC LIMIT 1;
```

---

### 7. Тест загрузки истории
**Шаги:**
1. Открыть дневник
2. Перейти на вкладку "История"
3. Выбрать дату в календаре

**Ожидаемый результат:**
- ✅ История загружается через RPC `get_diary_history`
- ✅ Отображаются записи за выбранную дату
- ✅ Записи сгруппированы по показателям
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM diary_history 
WHERE diary_id = '<diary_id>' 
AND DATE(occurred_at) = '<selected_date>'
ORDER BY occurred_at ASC;
```

---

### 8. Тест назначения сотрудника к дневнику (патронажное агентство)
**Шаги:**
1. Войти как патронажное агентство
2. Открыть дневник
3. Перейти в настройки → "Управление доступом"
4. Выбрать сотрудника из списка
5. Нажать "Добавить"

**Ожидаемый результат:**
- ✅ Доступ назначается через RPC `assign_employee_to_diary`
- ✅ Сотрудник появляется в списке назначенных
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM diary_employee_access 
WHERE diary_id = '<diary_id>' 
AND user_id = '<employee_user_id>'
AND revoked_at IS NULL;
```

---

### 9. Тест отзыва доступа сотрудника
**Шаги:**
1. В настройках дневника найти назначенного сотрудника
2. Нажать "Убрать доступ"

**Ожидаемый результат:**
- ✅ Доступ отзывается через RPC `remove_employee_from_diary`
- ✅ Сотрудник исчезает из списка назначенных
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT * FROM diary_employee_access 
WHERE diary_id = '<diary_id>' 
AND user_id = '<employee_user_id>'
AND revoked_at IS NOT NULL;
```

---

### 10. Тест автоматического назначения (пансионат)
**Шаги:**
1. Войти как пансионат
2. Создать дневник
3. Проверить настройки дневника → "Управление доступом"

**Ожидаемый результат:**
- ✅ Все сотрудники организации автоматически получают доступ
- ✅ Доступы создаются через RPC `assign_employee_to_diary`
- ✅ В консоли нет ошибок

**Проверка в Supabase:**
```sql
SELECT dea.*, oe.first_name, oe.last_name 
FROM diary_employee_access dea
JOIN organization_employees oe ON dea.user_id = oe.user_id
WHERE dea.diary_id = '<diary_id>' 
AND dea.revoked_at IS NULL;
```

---

## ⚠️ Важные замечания

1. **Fallback на localStorage:** Все функции имеют fallback на localStorage, если Supabase недоступен. Это позволяет работать в режиме разработки.

2. **Обратная совместимость:** Старые данные в localStorage будут использоваться как fallback, но новые данные сохраняются в Supabase.

3. **Ошибки в консоли:** При ошибках Supabase в консоли будут предупреждения, но приложение продолжит работать через localStorage.

4. **Тестирование на сервере:** Для полного тестирования необходимо:
   - Применить миграции на сервере
   - Настроить переменные окружения Supabase
   - Проверить подключение к базе данных

---

## 📝 Следующие шаги

После успешного прохождения всех тестов:
1. Удалить все обращения к localStorage (кроме fallback)
2. Обновить админ-панель для работы с Supabase (Шаг 16.5)
3. Провести финальное тестирование всего функционала

