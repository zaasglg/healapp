# Чек-лист готовности к деплою

**Дата проверки:** 15 ноября 2025

---

## ✅ КРИТИЧЕСКИЕ ФУНКЦИИ (должны работать)

### 1. Аутентификация и регистрация
- [x] **Регистрация организаций/сиделок** - работает через Supabase Auth ✅
- [x] **Регистрация сотрудников** - работает через Edge Function `accept_invite_token` ✅
- [x] **Регистрация клиентов** - работает через Edge Function `accept-invite` ✅
- [x] **Вход по email** (организации/сиделки) - работает через Supabase Auth ✅
- [x] **Вход по телефону** (сотрудники/клиенты) - работает через Supabase Auth ✅
- [x] **Подтверждение email** - работает через `EmailConfirmationPage` ✅
- [x] **Восстановление сессии** - работает через `authStore.checkAuth` ✅

**Статус:** ✅ ГОТОВО

---

### 2. Профили пользователей
- [x] **Создание профиля организации** - работает через RPC `create_organization` ✅
- [x] **Создание профиля сиделки** - работает через RPC `create_organization` ✅
- [x] **Создание профиля сотрудника** - работает через Edge Function ✅
- [x] **Создание профиля клиента** - работает через Edge Function ✅
- [x] **Редактирование профилей** - работает через Supabase (таблицы `organizations`, `organization_employees`, `clients`) ✅
- [x] **Загрузка профилей** - работает через Supabase ✅

**Статус:** ✅ ГОТОВО

---

### 3. Карточки подопечных
- [x] **Создание карточек** - работает через Supabase (таблица `patient_cards`) ✅
- [x] **Редактирование карточек** - работает через Supabase ✅
- [x] **Удаление карточек** - работает через Supabase ✅
- [x] **Загрузка карточек** - работает через Supabase ✅
- [x] **Права доступа** - проверяются через RLS политики ✅

**Примечание:** Черновики карточек хранятся в localStorage (это нормально, не критично)

**Статус:** ✅ ГОТОВО

---

### 4. Дневники подопечных
- [x] **Создание дневника** - работает через RPC `create_diary` ✅
- [x] **Загрузка дневников** - работает через Supabase (таблица `diaries`) ✅
- [x] **Загрузка метрик** - работает через Supabase (таблица `diary_metrics`) ✅
- [x] **Сохранение значений метрик** - работает через RPC `save_metric_value` ✅
- [x] **Загрузка истории** - работает через RPC `get_diary_history` ✅
- [x] **Редактирование метрик** - работает через Supabase ✅
- [x] **Настройки метрик** - работают через `diary_metrics.metadata.settings` ✅

**Статус:** ✅ ГОТОВО

---

### 5. Управление доступом
- [x] **Назначение сотрудников** - работает через RPC `assign_employee_to_diary` ✅
- [x] **Отзыв доступа сотрудников** - работает через RPC `remove_employee_from_diary` ✅
- [x] **Создание ссылок для клиентов** - работает через RPC `generate_invite_link` ✅
- [x] **Создание внешних ссылок** - работает через Supabase (таблица `diary_external_access_links`) ✅
- [x] **Принятие приглашений** - работает через Supabase ✅
- [x] **Обработка pending токенов** - работает через RPC `process_pending_diary_access` ✅

**Статус:** ✅ ГОТОВО

---

### 6. Приглашения
- [x] **Создание приглашений сотрудников** - работает через RPC `generate_invite_link` ✅
- [x] **Отзыв приглашений** - работает через RPC `revoke_invite_link` ✅
- [x] **Создание приглашений клиентов** - работает через RPC `generate_invite_link` ✅
- [x] **Загрузка приглашений** - работает через Supabase (таблица `invite_tokens`) ✅

**Статус:** ✅ ГОТОВО

---

## ⚠️ НЕКРИТИЧНЫЕ ФУНКЦИИ (можно доработать позже)

### 1. Админ-панель
- [ ] **Админ-панель** - работает на localStorage (временно)
- [ ] **Мониторинг** - работает на localStorage (временно)
- [ ] **Управление пользователями** - работает на localStorage (временно)

**Статус:** ⚠️ ВРЕМЕННО НА LOCALSTORAGE (не критично для деплоя)

**План:** Доработать после деплоя (Шаг 16.5)

---

## 🔧 ТЕХНИЧЕСКАЯ ПОДГОТОВКА

### 1. Переменные окружения
- [x] **VITE_SUPABASE_URL** - настроен в `src/lib/supabase.ts` ✅
- [x] **VITE_SUPABASE_ANON_KEY** - настроен в `src/lib/supabase.ts` ✅
- [ ] **Проверка на production** - нужно убедиться что переменные установлены на хостинге

**Что нужно сделать:**
1. Создать файл `.env.production` или настроить переменные на хостинге
2. Добавить `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY`

---

### 2. Миграции базы данных
- [x] **Все миграции созданы** - в папке `supabase/migrations/` ✅
- [ ] **Миграции применены на production Supabase** - нужно применить вручную

**Что нужно сделать:**
1. Зайти в Supabase Dashboard → SQL Editor
2. Применить все миграции из `supabase/migrations/` по порядку
3. Или использовать Supabase CLI: `supabase db push`

**Критические миграции:**
- ✅ Все таблицы созданы
- ✅ RLS политики настроены
- ✅ RPC функции созданы
- ✅ Edge Functions созданы

---

### 3. Edge Functions
- [x] **accept-invite** - создана и работает ✅
- [ ] **Деплой Edge Function на production** - нужно задеплоить

**Что нужно сделать:**
1. Задеплоить Edge Function через Supabase CLI:
   ```bash
   supabase functions deploy accept-invite
   ```
2. Или через Supabase Dashboard → Edge Functions

---

### 4. Сборка проекта
- [x] **npm run build** - работает ✅
- [x] **Структура dist/** - корректная ✅
- [ ] **Проверка размера бандла** - рекомендуется проверить

**Что нужно сделать:**
1. Запустить `npm run build`
2. Проверить что папка `dist/` создана
3. Проверить размер файлов (оптимизация)

---

### 5. GitHub Actions
- [x] **Workflow создан** - `.github/workflows/deploy.yml` ✅
- [x] **Настроен деплой через SSH** - готов ✅
- [ ] **Secrets настроены в GitHub** - нужно добавить

**Что нужно сделать:**
1. Добавить секреты в GitHub:
   - `SSH_HOST` - IP адрес VPS
   - `SSH_USERNAME` - логин (обычно `root`)
   - `SSH_PRIVATE_KEY` - приватный SSH ключ
   - `VITE_SUPABASE_URL` - URL Supabase проекта
   - `VITE_SUPABASE_ANON_KEY` - Anon key

---

## 📋 ФИНАЛЬНЫЙ ЧЕК-ЛИСТ ПЕРЕД ДЕПЛОЕМ

### Обязательно:
- [x] Все критические функции работают на Supabase ✅
- [ ] Переменные окружения настроены на production
- [ ] Миграции применены на production Supabase
- [ ] Edge Functions задеплоены на production
- [ ] Проект собирается без ошибок (`npm run build`)
- [ ] GitHub Secrets настроены

### Рекомендуется:
- [ ] Проверить размер бандла
- [ ] Протестировать на production Supabase (тестовый проект)
- [ ] Проверить работу всех RPC функций
- [ ] Проверить RLS политики

---

## 🚀 ГОТОВНОСТЬ К ДЕПЛОЮ

### Статус: ✅ ГОТОВО (с оговорками)

**Что готово:**
- ✅ Все критические функции работают на Supabase
- ✅ Код готов к сборке
- ✅ GitHub Actions настроен
- ✅ Документация по деплою готова

**Что нужно сделать перед деплоем:**
1. ⚠️ Применить миграции на production Supabase
2. ⚠️ Задеплоить Edge Functions на production
3. ⚠️ Настроить переменные окружения на хостинге
4. ⚠️ Настроить GitHub Secrets

**Что можно доработать после деплоя:**
- ⚠️ Админ-панель (перевести с localStorage на Supabase)

---

## 📝 ИНСТРУКЦИИ ПО ДЕПЛОЮ

См. файл: `docs/DEPLOY_SETUP_GUIDE.md`

---

## ⚠️ ВАЖНЫЕ ЗАМЕЧАНИЯ

1. **Админ-панель:** Работает на localStorage, это нормально для начала. Доработаем после деплоя.

2. **localStorage fallback:** В коде остались fallback на localStorage для обратной совместимости со старыми пользователями. Это не критично.

3. **Черновики:** Черновики карточек хранятся в localStorage - это нормально, не влияет на работу.

4. **Тестирование:** Рекомендуется протестировать на production Supabase перед финальным деплоем.

---

**Последнее обновление:** 15 ноября 2025

