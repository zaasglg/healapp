# Анализ соответствия 152-ФЗ и план миграции на российские серверы

> **ВАЖНО:** С 1 июля 2025 года вступает в силу полный запрет на хранение и обработку персональных данных граждан РФ за пределами страны (кроме исключений, предусмотренных законом).

## 📋 Анализ текущей ситуации

### Текущая архитектура:
- **Backend:** Supabase (серверы в Европе)
- **База данных:** PostgreSQL на Supabase (Европа)
- **Frontend:** Деплой через GitHub Pages/Vercel/Netlify
- **Edge Functions:** Supabase Edge Functions (Европа)
- **Storage:** Supabase Storage (Европа)

### Какие данные собираются:
1. **Персональные данные пользователей:**
   - ФИО (обязательно для всех ролей)
   - Телефон (обязательно для всех)
   - Email (для организаций и сиделок)
   - Дата рождения (для карточек подопечных)
   - Адрес (для организаций и карточек)

2. **Медицинские данные:**
   - Показатели здоровья (температура, давление, сатурация и т.д.)
   - История заполнения дневников
   - Диагнозы подопечных
   - Медицинские записи

3. **Данные организаций:**
   - Название организации
   - Адрес организации
   - Список сотрудников
   - Роли сотрудников

## ⚖️ Требования 152-ФЗ

### Основные требования:

1. **Статья 18.1 152-ФЗ:**
   - Персональные данные граждан РФ должны храниться на серверах, физически расположенных на территории РФ
   - Исключение: если субъект персональных данных дал согласие на обработку за пределами РФ

2. **Штрафы за нарушение:**
   - Для ИП: до 20 000 руб.
   - Для должностных лиц: до 75 000 руб.
   - Для юридических лиц: до 200 000 руб.
   - При повторном нарушении: до 500 000 руб.

3. **Роскомнадзор может:**
   - Заблокировать сайт
   - Потребовать удаления данных
   - Наложить административный штраф

### Попадаем ли мы под закон?

**ДА, попадаем**, потому что:
- ✅ Собираем персональные данные граждан РФ (ФИО, телефон, адрес)
- ✅ Собираем медицинские данные (показатели здоровья, диагнозы)
- ✅ Храним данные на серверах в Европе (Supabase)
- ✅ Не получаем явного согласия на хранение за пределами РФ

### Риски использования GitHub для деплоя фронтенда:

**✅ GitHub НЕ является проблемой для 152-ФЗ**, потому что:

1. **GitHub хранит только код:**
   - В репозитории находятся только исходные файлы (TypeScript, React компоненты)
   - Нет персональных данных пользователей
   - Нет данных из БД

2. **Фронтенд - статические файлы:**
   - После сборки (`npm run build`) получаются статические HTML/CSS/JS файлы
   - Эти файлы не содержат персональных данных
   - Персональные данные загружаются динамически через API при работе приложения

3. **Переменные окружения:**
   - `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` встраиваются в сборку
   - Это **не персональные данные**, а технические ключи доступа
   - Они не попадают под 152-ФЗ

4. **GitHub Actions (если используется):**
   - ✅ Используйте GitHub Secrets для хранения ключей
   - ✅ Не коммитьте `.env` файлы с реальными ключами
   - ✅ Убедитесь, что логи деплоя не содержат персональных данных
   - ✅ Используйте `.gitignore` для исключения чувствительных файлов

**Вывод:** GitHub можно использовать для деплоя фронтенда без проблем с 152-ФЗ, так как там не хранятся персональные данные.

## 🚀 План миграции на Timeweb Cloud

### Преимущества миграции:
1. ✅ **Соответствие 152-ФЗ:** Все данные на серверах в РФ
2. ✅ **Уже есть Docker на Timeweb:** Упрощает миграцию
3. ✅ **Полный контроль:** Собственный сервер, свои настройки
4. ✅ **Безопасность:** Данные не покидают территорию РФ
5. ✅ **Стоимость:** Контролируемые расходы

### Риски миграции:
1. ⚠️ **Простой сервиса:** Нужен план миграции без простоя
2. ⚠️ **Потеря данных:** Нужен бэкап и проверка целостности
3. ⚠️ **Изменение URL:** Нужно обновить переменные окружения
4. ⚠️ **Edge Functions:** Нужно переразвернуть на новом сервере

## 📝 Пошаговый план миграции

> **Преимущество:** У вас уже есть Docker на Timeweb хостинге, что значительно упрощает миграцию!

### Этап 1: Подготовка (1-2 дня)

1. **Создать полный бэкап текущей БД:**
   ```bash
   # Вариант 1: Через Supabase Dashboard
   # Settings → Database → Backups → Create backup
   
   # Вариант 2: Через Supabase CLI
   supabase db dump -f backup_$(date +%Y%m%d_%H%M%S).sql
   
   # Вариант 3: Через pg_dump напрямую (если есть доступ)
   pg_dump -h db.xxxxx.supabase.co -U postgres -d postgres > backup.sql
   ```

2. **Подготовить сервер на Timeweb Cloud:**
   - ✅ Docker уже установлен (по вашему сообщению)
   - Минимум: 2 CPU, 4 ГБ RAM, 20 ГБ диска (рекомендуется 4 CPU, 8 ГБ RAM)
   - Настроить домен и SSL-сертификат (Let's Encrypt бесплатно)
   - Открыть необходимые порты в firewall

3. **Подготовить файлы для миграции:**
   - ✅ Использовать `docker-compose.timeweb.yml` (уже есть в проекте)
   - ✅ Или использовать конфигурацию из статьи Timeweb
   - Подготовить `.env` файл с новыми ключами

### Этап 2: Развертывание Supabase на Timeweb (1 день)

**ВАЖНО:** Используйте готовую конфигурацию из статьи Timeweb, она уже протестирована и работает.

1. **Подключиться к серверу Timeweb:**
   ```bash
   ssh root@ваш_ip_адрес
   ```

2. **Создать директорию для Supabase:**
   ```bash
   mkdir -p ~/supabase
   cd ~/supabase
   ```

3. **Скачать готовую конфигурацию из статьи Timeweb:**
   ```bash
   # Создать директорию docker
   mkdir -p docker
   cd docker
   
   # Скачать конфигурацию
   wget -O docker-compose.yml https://raw.githubusercontent.com/tecspda/timeweb/main/supabase/docker-compose.yml
   wget -O .env https://raw.githubusercontent.com/tecspda/timeweb/main/supabase/.env
   ```

4. **Настроить переменные окружения в `.env`:**
   ```bash
   nano .env
   ```
   
   **Обязательно изменить:**
   - `POSTGRES_PASSWORD` - надежный пароль для БД
   - `JWT_SECRET` - сгенерировать новый (32+ символа)
   - `ANON_KEY` - сгенерировать новый
   - `SERVICE_ROLE_KEY` - сгенерировать новый
   - `SITE_URL` - ваш домен (например: `https://ваш-домен.ru`)
   - `API_EXTERNAL_URL` - ваш домен
   - `SUPABASE_PUBLIC_URL` - ваш домен
   
   **Генерация ключей (на вашем компьютере в PowerShell):**
   ```powershell
   # Выполните 3 раза, каждый раз копируйте результат:
   [Convert]::ToBase64String((1..32 | ForEach-Object { Get-Random -Maximum 256 }))
   ```

5. **Запустить контейнеры:**
   ```bash
   docker compose up -d
   ```

6. **Проверить статус:**
   ```bash
   docker compose ps
   # Все контейнеры должны быть в статусе "Up"
   ```

7. **Проверить работу:**
   - Открыть Studio: `http://ваш-домен:8000` (или порт из .env)
   - Проверить API: `http://ваш-домен:8000/rest/v1/`
   - Проверить логи: `docker compose logs`

### Этап 3: Миграция данных (1 день)

**ВАЖНО:** У вас уже есть все миграции в `supabase/migrations/` - это значительно упрощает процесс!

1. **Применить все миграции из вашего проекта:**
   ```bash
   # На сервере Timeweb, подключиться к БД
   docker exec -it caregivers-diary-db psql -U postgres -d postgres
   
   # Или применить миграции через файлы:
   # Скопировать все файлы из supabase/migrations/ на сервер
   # Применить их по порядку через psql
   ```

2. **Альтернативный способ - через Supabase CLI:**
   ```bash
   # На вашем компьютере
   # Связать локальный проект с новым Supabase
   supabase link --project-ref новый_project_ref
   
   # Применить все миграции
   supabase db push
   ```

3. **Импортировать данные из бэкапа:**
   ```bash
   # Скопировать backup.sql на сервер
   scp backup.sql root@ваш_ip:/root/
   
   # На сервере, восстановить данные
   docker exec -i caregivers-diary-db psql -U postgres -d postgres < /root/backup.sql
   ```

4. **Проверить целостность данных:**
   ```sql
   -- Подключиться к БД
   docker exec -it caregivers-diary-db psql -U postgres -d postgres
   
   -- Проверить количество записей
   SELECT 'organizations' as table_name, COUNT(*) FROM organizations
   UNION ALL
   SELECT 'clients', COUNT(*) FROM clients
   UNION ALL
   SELECT 'patient_cards', COUNT(*) FROM patient_cards
   UNION ALL
   SELECT 'diaries', COUNT(*) FROM diaries
   UNION ALL
   SELECT 'organization_employees', COUNT(*) FROM organization_employees;
   
   -- Проверить RLS политики
   SELECT schemaname, tablename, policyname FROM pg_policies WHERE schemaname = 'public';
   ```

### Этап 4: Миграция Edge Functions (1 день)

**ВАЖНО:** У вас уже есть Edge Functions в `supabase/functions/` - их нужно переразвернуть.

1. **Скопировать Edge Functions на сервер:**
   ```bash
   # На вашем компьютере
   scp -r supabase/functions root@ваш_ip:/root/supabase/
   ```

2. **Развернуть Edge Functions:**
   
   **Вариант 1: Через Supabase CLI (рекомендуется)**
   ```bash
   # На вашем компьютере
   # Связать с новым Supabase
   supabase link --project-ref новый_project_ref
   
   # Развернуть функции
   supabase functions deploy accept-invite
   supabase functions deploy create-admin-invite
   supabase functions deploy admin-users-data
   supabase functions deploy admin-support-data
   supabase functions deploy admin-revoke-invite
   ```
   
   **Вариант 2: Вручную через Docker (если CLI не работает)**
   ```bash
   # На сервере, скопировать функции в контейнер
   docker cp supabase/functions/accept-invite supabase-functions:/var/functions/accept-invite
   # И так для каждой функции
   ```

3. **Проверить работу функций:**
   ```bash
   # Проверить, что функции доступны
   curl http://ваш-домен:8000/functions/v1/accept-invite
   
   # Или через Supabase Studio
   # Открыть Studio → Edge Functions → проверить список
   ```

### Этап 5: Обновление фронтенда (1 день)

1. **Обновить переменные окружения:**
   ```env
   VITE_SUPABASE_URL=https://ваш-домен.ru
   VITE_SUPABASE_ANON_KEY=новый_anon_key
   ```

2. **Пересобрать фронтенд:**
   ```bash
   npm run build
   ```

3. **Обновить деплой:**
   - Обновить переменные на хостинге фронтенда
   - Пересобрать и задеплоить

### Этап 6: Тестирование (1-2 дня)

1. **Проверить все функции:**
   - Регистрация пользователей
   - Авторизация
   - Создание карточек и дневников
   - Заполнение показателей
   - Управление доступом
   - Edge Functions

2. **Проверить производительность:**
   - Скорость загрузки страниц
   - Время отклика API
   - Работа Realtime

### Этап 7: Переключение (1 день)

1. **Переключить DNS на новый сервер:**
   - Обновить A-запись домена
   - Дождаться распространения DNS (до 24 часов)

2. **Мониторинг:**
   - Следить за ошибками
   - Проверять логи
   - Отслеживать метрики

3. **Отключить старый Supabase:**
   - После подтверждения работы нового сервера
   - Сохранить финальный бэкап

## 🔒 Дополнительные меры безопасности

1. **Настроить SSL-сертификат:**
   - Использовать Let's Encrypt (бесплатно)
   - Или купить сертификат

2. **Настроить бэкапы:**
   - Автоматические ежедневные бэкапы БД
   - Хранение бэкапов на отдельном сервере

3. **Настроить мониторинг:**
   - Мониторинг доступности сервисов
   - Алерты при проблемах

4. **Документировать согласие:**
   - Добавить в пользовательское соглашение информацию о хранении данных в РФ
   - Получить согласие пользователей на обработку персональных данных

## ⚠️ Важные замечания

1. **Не удаляйте старый Supabase сразу:**
   - Оставьте на 1-2 недели для отката при проблемах
   - Убедитесь, что все работает стабильно

2. **Проверьте все интеграции:**
   - Если есть внешние сервисы, обновите их настройки
   - Проверьте webhooks и API ключи

3. **Сообщите пользователям:**
   - Уведомите о миграции (если требуется)
   - Объясните преимущества (безопасность, соответствие закону)

## 📊 Оценка времени и ресурсов

- **Время:** 5-7 рабочих дней
- **Ресурсы:** 
  - Сервер Timeweb Cloud (от 500 руб/мес)
  - Домен с SSL (если нет)
  - Время на тестирование

## ✅ Чеклист миграции

- [ ] Создан бэкап текущей БД
- [ ] Подготовлен сервер на Timeweb Cloud
- [ ] Развернут Supabase на новом сервере
- [ ] Применены все миграции
- [ ] Импортированы данные
- [ ] Развернуты Edge Functions
- [ ] Обновлен фронтенд
- [ ] Проведено тестирование
- [ ] Переключен DNS
- [ ] Проверена работа в production
- [ ] Настроены бэкапы
- [ ] Настроен мониторинг
- [ ] Обновлена документация

## 🎯 Итоговая рекомендация

### ✅ НУЖНО МИГРИРОВАТЬ на российские серверы

**Причины:**
1. ✅ **Соответствие 152-ФЗ:** С 1 июля 2025 полный запрет на хранение ПД за пределами РФ
2. ✅ **Избежание штрафов:** До 200 000 руб. (до 500 000 при повторном нарушении)
3. ✅ **Безопасность данных:** Полный контроль над инфраструктурой
4. ✅ **Юридическая защита:** Соответствие российскому законодательству

### ✅ GitHub для деплоя фронтенда - НЕ ПРОБЛЕМА

**Почему:**
- GitHub хранит только код (не персональные данные)
- Фронтенд - статические файлы (не содержат ПД)
- Персональные данные загружаются динамически через API
- Переменные окружения (ключи API) не являются персональными данными

**Можно продолжать использовать GitHub** для:
- Хранения кода
- CI/CD через GitHub Actions
- Деплоя фронтенда

### ✅ План миграции безопасен и выполним

**Преимущества вашей ситуации:**
- ✅ Уже есть Docker на Timeweb (упрощает миграцию)
- ✅ Есть все миграции БД в `supabase/migrations/`
- ✅ Есть Edge Functions в `supabase/functions/`
- ✅ Есть готовая конфигурация `docker-compose.timeweb.yml`
- ✅ Есть подробная инструкция от Timeweb

**Риски минимальны**, если:
- ✅ Следовать пошаговому плану
- ✅ Делать бэкапы на каждом этапе
- ✅ Тестировать перед переключением
- ✅ Иметь план отката (старый Supabase оставить на 1-2 недели)

### 📅 Рекомендуемые сроки

**Оптимально начать миграцию:**
- **До 1 июля 2025** - чтобы успеть до вступления в силу полного запрета
- **Сейчас** - чтобы иметь запас времени на тестирование

**Время миграции:** 5-7 рабочих дней (с учетом тестирования)

### 🔐 Дополнительные рекомендации

1. **Добавить в пользовательское соглашение:**
   - Информацию о хранении данных в РФ
   - Получение согласия на обработку ПД

2. **Настроить автоматические бэкапы:**
   - Ежедневные бэкапы БД
   - Хранение на отдельном сервере

3. **Настроить мониторинг:**
   - Отслеживание доступности сервисов
   - Алерты при проблемах

4. **Документировать миграцию:**
   - Записать все шаги
   - Сохранить конфигурации
   - Документировать изменения

