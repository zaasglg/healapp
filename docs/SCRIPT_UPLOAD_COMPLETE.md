# ✅ Скрипт загружен на сервер

## Проблема решена

**Причина:** GitHub недоступен на сервере (`Temporary failure in name resolution`)

**Решение:** Файл загружен вручную через `scp`

## ✅ Что сделано

1. ✅ Файл `RF_server_SupaBase.sh` загружен на сервер
2. ✅ Файл сделан исполняемым (`chmod +x`)
3. ✅ Скрипт готов к выполнению

## 🚀 Запуск скрипта

**На сервере** выполните:

```bash
/root/RF_server_SupaBase.sh
```

Скрипт запросит:
- IP или домен
- Email для SSL
- Имя пользователя для входа
- Пароль для входа

## 📝 Важно

Скрипт **НЕ устанавливает Docker** - только проверяет его наличие. На вашем сервере Docker уже установлен (версия 29.0.0), поэтому скрипт пройдет проверку и продолжит установку Supabase.

## ⚠️ Если GitHub недоступен

Скрипт попытается клонировать репозиторий Supabase с GitHub. Если это не удастся, скрипт выдаст ошибку и предложит альтернативы.

## 🔄 Альтернатива

Если клонирование не удастся, можно:
1. Скачать архив репозитория вручную
2. Загрузить на сервер через `scp`
3. Распаковать и использовать

