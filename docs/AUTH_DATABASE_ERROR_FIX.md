# 🔧 Исправление ошибки "Database error querying schema"

## Проблема

Ошибка 500: `Database error querying schema`

Это означает, что Auth сервис не может найти таблицы в схеме `auth`.

## Возможные причины

1. Таблица `auth.users` не существует
2. Неправильно настроен `GOTRUE_DB_NAMESPACE`
3. Проблемы с подключением к БД
4. Миграции Auth не применены

## Решение

Проверяем и исправляем:

1. Проверяем наличие схемы `auth` и таблиц
2. Проверяем настройки `GOTRUE_DB_NAMESPACE`
3. Перезапускаем Auth сервис
4. Проверяем логи на наличие ошибок

## Статус

Проверяем конфигурацию и исправляем проблему...

