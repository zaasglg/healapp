# 📋 Cloud-init скрипт для сервера с Portainer

## Что делает скрипт

Скрипт `cloud-init-supabase.sh` подготовит сервер для установки Supabase:

1. ✅ **Обновит систему** и установит необходимые утилиты
2. ✅ **Настроит Git** для работы с GitHub (через HTTP)
3. ✅ **Настроит DNS** (если нужно)
4. ✅ **Создаст директории** для Supabase
5. ✅ **Настроит базовый фаервол**
6. ✅ **Проверит Docker и Portainer**
7. ✅ **Создаст скрипт установки** Supabase (`/opt/supabase-project/install_supabase.sh`)

## ⚠️ Важно

**Скрипт НЕ устанавливает:**
- ❌ Docker (уже установлен через Portainer)
- ❌ Portainer (уже установлен)
- ❌ Supabase (будет установлен позже вручную)

**Скрипт только ПОДГОТАВЛИВАЕТ окружение!**

## 📝 Использование

### Шаг 1: Загрузите скрипт в Cloud-init

При заказе сервера в разделе **"7. Cloud-init"**:
1. Скопируйте содержимое файла `cloud-init-supabase.sh`
2. Вставьте в текстовое поле Cloud-init
3. Или загрузите файл через кнопку "Загрузить скрипт из файла"

### Шаг 2: После загрузки сервера

1. **Подождите** завершения выполнения Cloud-init (2-5 минут)
2. **Проверьте логи:**
   ```bash
   cat /var/log/cloud-init-output.log
   ```

3. **Откройте Portainer:**
   - Обычно доступен по IP:9000 или через веб-интерфейс провайдера

4. **Запустите установку Supabase:**
   ```bash
   /opt/supabase-project/install_supabase.sh
   ```

### Шаг 3: Настройка через Portainer

После установки Supabase через скрипт:
- Все контейнеры будут видны в Portainer
- Можно управлять через веб-интерфейс Portainer
- Можно настроить сети, volumes, и т.д.

## 🔧 Что будет установлено

- ✅ curl, git, jq
- ✅ nginx, certbot
- ✅ apache2-utils (для Basic Auth)
- ✅ unzip, htop, net-tools
- ✅ ufw (фаервол)

## 📁 Структура после Cloud-init

```
/opt/supabase-project/
├── install_supabase.sh  # Скрипт для установки Supabase
└── backups/             # Директория для бэкапов
```

## ⚙️ Настройки Git

Git настроен для работы через HTTP (если HTTPS заблокирован):
- Автоматическая замена HTTPS → HTTP
- Отключена проверка SSL
- Увеличены таймауты

## 🔒 Безопасность

- Фаервол настроен (UFW)
- SSH разрешен
- HTTP/HTTPS разрешены
- Остальные порты закрыты

## 📝 После установки

После выполнения Cloud-init и установки Supabase:
1. Настройте Nginx для Supabase
2. Настройте SSL сертификат
3. Настройте Basic Auth для Studio
4. Перенесите бэкапы и миграции

