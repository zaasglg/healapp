# ✅ Supabase готов к работе!

## 🎉 Статус

Supabase успешно восстановлен и настроен на сервере `176.124.217.224`.

### Работающие сервисы:
- ✅ **PostgreSQL** (база данных) - порт 54322
- ✅ **REST API** (PostgREST) - порт 54321
- ✅ **Auth API** (GoTrue) - порт 54324
- ✅ **Nginx** (обратный прокси) - порт 80

## 🔗 Адреса для подключения

### Основной URL (для Supabase клиента):
```
http://176.124.217.224
```

### API эндпоинты:
- **REST API:** `http://176.124.217.224/rest/v1/`
- **Auth API:** `http://176.124.217.224/auth/v1/`
- **Health Check:** `http://176.124.217.224/auth/health`

## 🔑 Ключи доступа

### ANON_KEY (публичный ключ):
```
5a35400515a186b111c2bb2b8f37154000e1d631d12d7a227dc76dea01af2184
```

### SERVICE_ROLE_KEY (приватный ключ - только для сервера):
```
78013cffaafbb367925be0f1142e4c76c223a015946096e38ca01bb31abcee2b
```

**⚠️ ВАЖНО:** SERVICE_ROLE_KEY не используйте на фронтенде! Только на бэкенде.

## ⚙️ Настройка фронтенда

В файле `.env.local` или `.env.production`:

```env
VITE_SUPABASE_URL=http://176.124.217.224
VITE_SUPABASE_ANON_KEY=5a35400515a186b111c2bb2b8f37154000e1d631d12d7a227dc76dea01af2184
```

## 📝 Проверка работоспособности

### 1. Проверка через браузер:
Откройте: `http://176.124.217.224/`
Должно показать: "Supabase API is running"

### 2. Проверка REST API:
```bash
curl http://176.124.217.224/rest/v1/
```
Должен вернуть: `{}`

### 3. Проверка Auth API:
```bash
curl http://176.124.217.224/auth/health
```
Должен вернуть информацию о GoTrue.

## 🗄️ Подключение к базе данных

**PostgreSQL:**
- Host: `176.124.217.224`
- Port: `54322`
- Database: `postgres`
- User: `postgres`
- Password: `4a83705eeb44c26e2ff867a5f853f5ce`

**Строка подключения:**
```
postgresql://postgres:4a83705eeb44c26e2ff867a5f853f5ce@176.124.217.224:54322/postgres
```

## 📁 Расположение файлов на сервере

- **Docker Compose:** `/opt/supabase-project/supabase/docker/docker-compose.yml`
- **Конфигурация:** `/opt/supabase-project/supabase/docker/.env`
- **Nginx конфигурация:** `/etc/nginx/sites-enabled/supabase`

## 🚀 Управление сервисами

### Перезапуск всех сервисов:
```bash
ssh root@176.124.217.224
cd /opt/supabase-project/supabase/docker
docker compose restart
```

### Просмотр логов:
```bash
docker compose logs -f
```

### Остановка:
```bash
docker compose down
```

### Запуск:
```bash
docker compose up -d
```

## ✅ Что дальше?

1. **Подключите фронтенд** - используйте URL и ANON_KEY выше
2. **Создайте таблицы** - через миграции или вручную
3. **Настройте SSL** - для production рекомендуется HTTPS
4. **Восстановите данные** - если есть бэкапы

## 🔒 Безопасность

- ✅ CORS настроен для работы с фронтендом
- ⚠️ SSL не настроен (рекомендуется для production)
- ⚠️ Пароли и ключи хранятся в `.env` файле

## 📞 Поддержка

Все файлы конфигурации находятся на сервере в `/opt/supabase-project/supabase/docker/`

