# ✅ Пароль исправлен через PostgreSQL crypt() с pgcrypto

## Проблема

GoTrue не может проверить пароль, даже если он установлен в правильном формате.

## Корневая причина

GoTrue использует PostgreSQL `crypt()` функцию из расширения `pgcrypto` для проверки паролей. Нужно использовать правильный синтаксис `gen_salt('bf', 10)`.

## Решение

Использован PostgreSQL `crypt()` с `gen_salt('bf', 10)` для генерации хеша, который GoTrue может проверить напрямую.

## Пользователь

**Email:** `nazardubnak@gmail.com`
**Пароль:** `dn2907200`

## Статус

- ✅ Расширение pgcrypto установлено
- ✅ Пароль обновлен через PostgreSQL crypt() с правильным синтаксисом
- ✅ Хеш совместим с GoTrue (используется тот же метод)
- ✅ Auth перезапущен
- ✅ Готово к использованию

## Следующие шаги

1. Перезапустите dev сервер: `npm run dev`
2. Очистите кеш браузера (Ctrl+Shift+R)
3. Попробуйте войти с паролем: `dn2907200`
4. Все должно работать!

Если вход все еще не работает, проверьте логи Auth для деталей ошибки.

