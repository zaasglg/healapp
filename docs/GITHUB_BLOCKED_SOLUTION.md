# ❌ Проблема: GitHub недоступен на сервере

## Диагностика

**Ошибка:** `ping: github.com: Temporary failure in name resolution`

Это означает, что сервер не может подключиться к GitHub.

## ✅ Решение

### Вариант 1: Загрузить файл вручную (рекомендуется)

**На вашей Windows машине** выполните:

```bash
scp RF_server_SupaBase.sh root@176.124.217.224:/root/RF_server_SupaBase.sh
```

**На сервере** выполните:

```bash
chmod +x /root/RF_server_SupaBase.sh
/root/RF_server_SupaBase.sh
```

### Вариант 2: Использовать зеркало GitHub

Если есть доступ к зеркалу:

```bash
curl -s https://ghproxy.com/https://raw.githubusercontent.com/kalininlive/supabase-vds-install/main/install.sh | bash
```

### Вариант 3: Создать файл вручную на сервере

**На сервере** выполните:

```bash
nano /root/RF_server_SupaBase.sh
```

Затем скопируйте содержимое файла `RF_server_SupaBase.sh` в редактор, сохраните (Ctrl+O, Enter, Ctrl+X) и выполните:

```bash
chmod +x /root/RF_server_SupaBase.sh
/root/RF_server_SupaBase.sh
```

### Вариант 4: Использовать VPN или прокси

Если у вас есть VPN или прокси, настройте его на сервере перед выполнением команды.

## 📝 Текущий статус

- ✅ Файл `RF_server_SupaBase.sh` создан и готов
- ❌ GitHub недоступен на сервере
- ✅ Решение: загрузить файл вручную через `scp`

## 🚀 Следующие шаги

1. Загрузите файл `RF_server_SupaBase.sh` на сервер через `scp`
2. Сделайте его исполняемым: `chmod +x`
3. Запустите: `/root/RF_server_SupaBase.sh`

