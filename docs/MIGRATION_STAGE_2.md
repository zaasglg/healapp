# Этап 2: Развертывание Supabase на сервере

## 📋 Предварительные требования

- ✅ Этап 0 завершен (бэкап создан)
- ✅ Этап 1 завершен (сервер подготовлен, .env создан)

---

## 📋 Шаг 1: Получение миграций из Supabase

**⚠️ ВАЖНО:** Получаем миграции из текущего Supabase, а не из локального проекта!

### Вариант 1: Через Supabase CLI (рекомендуется)

**На вашем компьютере:**

```powershell
# 1. Установить Supabase CLI (если еще не установлен)
npm install -g supabase

# 2. Авторизоваться
supabase login

# 3. Связать проект
supabase link --project-ref mtpawypaihmwrngirnxa

# 4. Получить текущую схему БД
supabase db dump -f supabase/migrations/from_supabase/current_schema.sql

# 5. Или получить список примененных миграций
supabase migration list
```

### Вариант 2: Через Supabase Dashboard

1. Откройте: https://mtpawypaihmwrngirnxa.supabase.co
2. Перейдите в **Database** → **Migrations**
3. Экспортируйте все миграции
4. Сохраните в `supabase/migrations/from_supabase/`

### Вариант 3: Через SQL Editor

1. Откройте Supabase Dashboard → **SQL Editor**
2. Выполните запрос для получения схемы:

```sql
-- Получить список всех таблиц
SELECT table_name 
FROM information_schema.tables 
WHERE table_schema = 'public'
ORDER BY table_name;

-- Экспортировать схему через pg_dump (на вашем компьютере)
-- Используйте connection string из Settings → Database
```

---

## 📋 Шаг 2: Копирование файлов на сервер

**На вашем компьютере** (в PowerShell):

```powershell
# Перейти в корень проекта
cd "C:\Users\User\Desktop\Веб версия дневник"

# Скопировать docker-compose
scp docker-compose.production.yml root@176.124.217.224:~/supabase/docker-compose.yml

# Скопировать kong.yml
scp kong.yml root@176.124.217.224:~/supabase/kong.yml

# Скопировать миграции (если получили через CLI)
scp -r supabase/migrations/from_supabase root@176.124.217.224:~/supabase/migrations/

# Скопировать Edge Functions
scp -r supabase/functions root@176.124.217.224:~/supabase/
```

---

## 📋 Шаг 3: Подключение к серверу и проверка файлов

**На сервере:**

```bash
ssh root@176.124.217.224
cd ~/supabase

# Проверить наличие файлов
ls -la

# Должны быть:
# - docker-compose.yml
# - .env
# - kong.yml
# - migrations/ (если скопировали)
# - functions/ (если скопировали)
```

---

## 📋 Шаг 4: Создание директорий для volumes

```bash
# Создать директории для данных
mkdir -p ~/supabase/data/postgres
mkdir -p ~/supabase/data/storage
mkdir -p ~/supabase/functions

# Установить правильные права
chmod 755 ~/supabase/data
chmod 755 ~/supabase/data/postgres
chmod 755 ~/supabase/data/storage
```

---

## 📋 Шаг 5: Обновление docker-compose.yml для использования локальных volumes

Если нужно использовать локальные директории вместо Docker volumes, отредактируйте `docker-compose.yml`:

```bash
nano docker-compose.yml
```

Найдите секции `volumes:` и замените на:

```yaml
volumes:
  postgres-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /root/supabase/data/postgres
  storage-data:
    driver: local
    driver_opts:
      type: none
      o: bind
      device: /root/supabase/data/storage
```

**Или оставьте как есть** - Docker volumes будут созданы автоматически.

---

## 📋 Шаг 6: Запуск контейнеров

```bash
cd ~/supabase

# Проверить конфигурацию
docker compose config

# Запустить контейнеры в фоновом режиме
docker compose up -d

# Следить за логами
docker compose logs -f
```

**Ожидаемое время запуска:** 2-5 минут

---

## 📋 Шаг 7: Проверка статуса контейнеров

```bash
# Проверить статус всех контейнеров
docker compose ps

# Все контейнеры должны быть в статусе "Up" и "healthy"
```

**Если контейнер не запускается:**

```bash
# Посмотреть логи конкретного контейнера
docker compose logs db
docker compose logs auth
docker compose logs kong

# Перезапустить контейнер
docker compose restart db
```

---

## 📋 Шаг 8: Проверка работы сервисов

### Проверка API:

```bash
# Проверить Kong (API Gateway)
curl http://localhost:54321/rest/v1/

# Должен вернуть JSON с информацией об API
```

### Проверка Studio:

Откройте в браузере:
```
http://176.124.217.224:54324
```

**Если Studio не открывается:**
- Проверьте firewall: `ufw allow 54324/tcp`
- Проверьте логи: `docker compose logs studio`

### Проверка БД:

```bash
# Подключиться к БД
docker exec -it caregivers-diary-db psql -U postgres -d postgres

# Выполнить тестовый запрос
SELECT version();

# Выйти
\q
```

---

## 📋 Шаг 9: Получение реальных ключей из Studio

**⚠️ ВАЖНО:** После первого запуска нужно получить реальные `ANON_KEY` и `SERVICE_ROLE_KEY`!

1. Откройте Studio: `http://176.124.217.224:54324`
2. Перейдите в **Settings** → **API**
3. Скопируйте:
   - **anon/public key** → это `ANON_KEY`
   - **service_role key** → это `SERVICE_ROLE_KEY`
4. Обновите `.env` файл:

```bash
nano .env
```

Замените значения на реальные ключи из Studio.

5. Перезапустите контейнеры:

```bash
docker compose restart
```

---

## ✅ Чеклист Этапа 2

- [ ] Получены миграции из текущего Supabase
- [ ] Файлы скопированы на сервер
- [ ] Созданы директории для volumes
- [ ] Запущены все контейнеры
- [ ] Все контейнеры в статусе "Up" и "healthy"
- [ ] API доступен (curl http://localhost:54321/rest/v1/)
- [ ] Studio открывается в браузере
- [ ] БД доступна (можно подключиться через psql)
- [ ] Получены реальные ключи из Studio
- [ ] Обновлен .env файл с реальными ключами
- [ ] Контейнеры перезапущены с новыми ключами

---

## 🚀 Следующий шаг

После завершения Этапа 2, сообщите мне, и я подготовлю **Этап 3: Применение миграций БД**.

---

## ❓ Проблемы?

**Контейнеры не запускаются:**
- Проверьте логи: `docker compose logs`
- Проверьте, что порты свободны: `netstat -tuln | grep 54321`
- Проверьте .env файл на наличие всех переменных

**Studio не открывается:**
- Проверьте firewall: `ufw status`
- Откройте порт: `ufw allow 54324/tcp`
- Проверьте логи: `docker compose logs studio`

**БД не работает:**
- Проверьте логи: `docker compose logs db`
- Проверьте, что пароль правильный в .env
- Проверьте volumes: `docker volume ls`

