# Статус сервисов после перезагрузки

## Текущий статус

### ✅ Работают

- ✅ **БД**: работает (healthy)
- ✅ **REST API**: работает (возвращает данные)
- ✅ **Functions**: работают
- ✅ **Meta**: работает (healthy)

### ⚠️ Требует внимания

- ⚠️ **Auth**: исправляются миграции БД
  - Первая миграция исправлена (provider_id)
  - Вторая миграция исправляется (last_sign_in_at)
  - После исправления всех миграций Auth должен запуститься

### 📊 Проверка работы

**REST API работает:**
```bash
curl http://176.124.217.224/rest/v1/ -H 'apikey: ...'
# Возвращает: {}
```

**Functions работают:**
```bash
curl http://176.124.217.224/functions/v1/accept-invite
# Работает
```

**Auth:**
- Исправляются миграции
- После исправления будет доступен на `http://176.124.217.224/auth/v1/`

## Фронтенд

Фронтенд настроен через `.env.local`:
```env
VITE_SUPABASE_URL=http://176.124.217.224
VITE_SUPABASE_ANON_KEY=kSW54FYXXViT8ifRUef7DqfNXYp687ecDjdp8HSggodn1To2638caajJMoA48qze
```

**REST API и Functions работают, Auth исправляется.**

