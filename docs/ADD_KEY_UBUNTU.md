# Добавление SSH ключа на Ubuntu сервер

## 🎯 Вариант 1: Все команды одной строкой

Скопируй и выполни **одну команду** в консоли Ubuntu:

```bash
mkdir -p ~/.ssh && chmod 700 ~/.ssh && echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG" >> ~/.ssh/authorized_keys && chmod 600 ~/.ssh/authorized_keys && cat ~/.ssh/authorized_keys
```

Эта команда:
1. Создаст директорию `.ssh`
2. Установит права на директорию
3. Добавит ключ в `authorized_keys`
4. Установит права на файл
5. Покажет содержимое файла (для проверки)

---

## 🎯 Вариант 2: По одной команде (если первая не работает)

Если первая команда не работает, выполни по очереди:

### 1. Создаем директорию
```bash
mkdir -p ~/.ssh
```

### 2. Права на директорию
```bash
chmod 700 ~/.ssh
```

### 3. Добавляем ключ
```bash
echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG" >> ~/.ssh/authorized_keys
```

### 4. Права на файл
```bash
chmod 600 ~/.ssh/authorized_keys
```

### 5. Проверяем
```bash
cat ~/.ssh/authorized_keys
```

---

## ✅ Что должно получиться

После выполнения должна появиться строка:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG
```

---

## 🔍 Если возникли ошибки

**Ошибка "Permission denied":**
- Убедись, что ты залогинен как `root`
- Проверь: `whoami` (должно быть `root`)

**Ошибка "No such file or directory":**
- Команда `mkdir -p ~/.ssh` должна создать директорию
- Проверь: `ls -la ~/.ssh`

**Ключ не добавился:**
- Проверь содержимое: `cat ~/.ssh/authorized_keys`
- Если файл пустой, попробуй еще раз команду с `echo`

