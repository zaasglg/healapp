# ✅ Скрипт успешно загружен на сервер

## Проблема

**Причина:** GitHub недоступен на сервере, поэтому команда `bash <(curl -s ...)` не работает.

## ✅ Решение

Файл `RF_server_SupaBase.sh` загружен на сервер через SSH перенаправление.

## 🚀 Запуск скрипта

**На сервере** выполните:

```bash
/root/RF_server_SupaBase.sh
```

## 📝 Что запросит скрипт

1. **IP или домен** (например: `176.124.217.224` или `supabase.healapp.ru`)
2. **Email для SSL** (для Let's Encrypt сертификата)
3. **Имя пользователя** (для входа в Supabase Studio)
4. **Пароль** (для входа в Supabase Studio)

## ⚠️ Важно

1. **Docker не будет установлен** - скрипт только проверит его наличие
2. **GitHub может быть недоступен** - скрипт попытается клонировать репозиторий, но если не получится, выдаст ошибку
3. **Если клонирование не удастся**, нужно будет скачать репозиторий вручную

## 🔄 Если GitHub недоступен

Если скрипт не сможет клонировать репозиторий Supabase, можно:

1. Скачать архив с GitHub на вашу Windows машину
2. Загрузить на сервер через `scp`
3. Распаковать и использовать

Или использовать зеркало GitHub (если доступно).

