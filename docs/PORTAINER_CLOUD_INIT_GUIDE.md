# 📋 Cloud-init для сервера с Portainer

## ✅ Да, нужно загрузить скрипт в Cloud-init!

### Что делает скрипт

Скрипт `cloud-init-portainer-minimal.sh` **только подготавливает окружение**:

1. ✅ Обновляет систему
2. ✅ Устанавливает утилиты (git, nginx, certbot и т.д.)
3. ✅ Настраивает Git для работы с GitHub
4. ✅ Настраивает DNS
5. ✅ Создает директории для Supabase
6. ✅ Настраивает базовый фаервол
7. ✅ Проверяет Docker и Portainer

### ❌ Что скрипт НЕ делает

- ❌ Не устанавливает Docker (уже установлен через Portainer)
- ❌ Не устанавливает Portainer (уже установлен)
- ❌ Не устанавливает Supabase (будет установлен позже)

## 📝 Инструкция по использованию

### Шаг 1: Загрузите скрипт в Cloud-init

При заказе сервера в разделе **"7. Cloud-init"**:

1. **Откройте файл** `cloud-init-portainer-minimal.sh`
2. **Скопируйте весь его содержимое**
3. **Вставьте в текстовое поле** Cloud-init в интерфейсе заказа
4. Или используйте кнопку **"Загрузить скрипт из файла"**

### Шаг 2: После загрузки сервера

1. **Подождите 2-5 минут** (выполнение Cloud-init)
2. **Проверьте логи:**
   ```bash
   cat /var/log/cloud-init-output.log
   ```

3. **Откройте Portainer:**
   - Обычно доступен по `http://ваш-ip:9000`
   - Или через веб-интерфейс провайдера

### Шаг 3: Установка Supabase

**Вариант A: Через командную строку**

```bash
cd /opt/supabase-project
git clone --depth 1 http://github.com/supabase/supabase.git
cd supabase/docker
cp .env.example .env
# Отредактируйте .env файл
nano .env
# Запустите
docker compose up -d
```

**Вариант B: Через Portainer (рекомендуется)**

1. Откройте Portainer
2. Перейдите в **Stacks** → **Add stack**
3. Назовите stack: `supabase`
4. Загрузите `docker-compose.yml` из репозитория Supabase
5. Настройте переменные окружения
6. Запустите stack

## 🔧 Настройка контейнеров через Portainer

После установки Supabase:

1. **Все контейнеры будут видны в Portainer:**
   - db (PostgreSQL)
   - auth (GoTrue)
   - rest (PostgREST)
   - storage
   - realtime
   - meta
   - studio
   - functions

2. **Можно управлять через веб-интерфейс:**
   - Просмотр логов
   - Перезапуск контейнеров
   - Настройка сетей
   - Управление volumes
   - Мониторинг ресурсов

## 📁 Структура после Cloud-init

```
/opt/supabase-project/
├── README.md           # Инструкции по установке
└── backups/           # Директория для бэкапов
```

## ⚙️ Что будет установлено

- ✅ curl, git, jq
- ✅ nginx, certbot
- ✅ apache2-utils (для Basic Auth)
- ✅ unzip, htop, net-tools
- ✅ ufw (фаервол)
- ✅ openssl

## 🔒 Безопасность

- Фаервол настроен (UFW)
- SSH разрешен
- HTTP/HTTPS разрешены
- Остальные порты закрыты

## 📝 Рекомендации

1. **Используйте Cloud-init** - это сэкономит время на настройке
2. **Установите Supabase через Portainer** - удобнее управлять через веб-интерфейс
3. **Сохраните все пароли** - они понадобятся для подключения
4. **Настройте бэкапы** - регулярно делайте резервные копии

## ✅ Итого

**Да, загрузите скрипт в Cloud-init!** Он подготовит сервер, а установку Supabase сделаете потом через Portainer или командную строку.

