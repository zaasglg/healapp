# Настройка SSH ключей в Reg.cloud

## 📋 Что нужно добавить в форму

### Твой публичный SSH ключ:
```
ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG
```

---

## 🎯 Пошаговая инструкция

### Шаг 1: Добавление SSH ключа в Reg.cloud

1. **В панели Reg.cloud** (где ты сейчас):
   - Ты в разделе **"Настройки"** → **"Настройки окружения"**
   - Видишь секцию **"SSH-ключи"**
   - Нажми кнопку **"+ Добавить SSH-ключ"**

2. **В форме добавления:**
   - **Название ключа**: `github-actions-deploy` (или любое удобное имя)
   - **SSH-ключ**: Вставь эту строку:
     ```
     ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG
     ```
   - Нажми **"Добавить"** или **"Сохранить"**

3. **Проверка:**
   - Ключ должен появиться в списке SSH-ключей

---

### Шаг 2: Привязка ключа к серверу

После добавления ключа в панели, нужно привязать его к твоему VPS серверу:

1. **Найди свой VPS сервер** в панели Reg.cloud:
   - Перейди в раздел с серверами (иконка с сеткой или "Серверы")
   - Выбери свой VPS (IP: 89.111.154.27)

2. **Привязка ключа:**
   - В настройках сервера найди раздел **"SSH-ключи"** или **"Доступ"**
   - Выбери добавленный ключ `github-actions-deploy`
   - Сохрани изменения

   **ИЛИ** (если такой опции нет):
   - Ключ автоматически доступен для всех серверов в этом проекте
   - Нужно будет добавить его вручную на сервер (через консоль или API)

---

### Шаг 3: Добавление ключа на сервер (если нужно)

Если ключ не добавился автоматически на сервер, нужно добавить его вручную:

1. **Подключись к серверу** через веб-консоль Reg.cloud:
   - Найди свой сервер
   - Нажми **"Консоль"** или **"VNC"**

2. **Выполни команды:**
   ```bash
   # Создаем директорию (если еще нет)
   mkdir -p ~/.ssh
   chmod 700 ~/.ssh
   
   # Добавляем ключ
   echo "ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIAqV6yiw499ncx5OCHZ8qLFVvnZ7w2iorBB0CG6G+QB5 user@DESKTOP-K2HODEG" >> ~/.ssh/authorized_keys
   
   # Устанавливаем права
   chmod 600 ~/.ssh/authorized_keys
   
   # Проверяем
   cat ~/.ssh/authorized_keys
   ```

---

## 🔍 Как проверить, что ключ работает

### Тест подключения с локальной машины:

```powershell
# В PowerShell на твоем компьютере
ssh root@89.111.154.27
```

Если подключение прошло без пароля - ключ работает! ✅

---

## 📝 Важные моменты

1. **Публичный ключ** добавляется в панель Reg.cloud
2. **Приватный ключ** (`id_ed25519`) останется на твоем компьютере - его нужно будет добавить в GitHub Secrets
3. **Ключ должен быть добавлен на сервер** в файл `~/.ssh/authorized_keys`

---

## 🚀 Что дальше

После успешного добавления ключа:
1. ✅ Проверим подключение
2. ✅ Установим Nginx на сервере
3. ✅ Настроим GitHub Secrets
4. ✅ Настроим автоматический деплой

