# ✅ Деплой успешен! Что дальше?

## 🎉 Поздравляю!

Твой сайт успешно задеплоен на сервер! Все работает автоматически:
- ✅ При каждом `git push` в `main` ветку сайт автоматически обновляется
- ✅ GitHub Actions собирает проект и загружает на сервер
- ✅ Nginx автоматически перезагружается

---

## 🔍 Шаг 1: Проверь сайт

### Открой в браузере:
```
http://89.111.154.27
```

**Что должно быть:**
- ✅ Сайт открывается
- ✅ Нет ошибок в консоли браузера (F12)
- ✅ Страницы переключаются (роутинг работает)
- ✅ Можно залогиниться/зарегистрироваться

**Если что-то не работает:**
- Проверь логи Nginx на сервере: `tail -f /var/log/nginx/error.log`
- Проверь, что файлы на месте: `ls -la /var/www/diary-app`

---

## 🌐 Шаг 2: Настрой домен (если есть)

### Если у тебя есть домен (например, `diary-app.ru`):

1. **Зайди в панель управления доменом** (где покупал домен)
2. **Найди настройки DNS**
3. **Добавь A-запись:**
   - **Тип:** `A`
   - **Имя:** `@` (или оставь пустым для корневого домена)
   - **Значение:** `89.111.154.27`
   - **TTL:** `3600` (или автоматически)

4. **Если нужен поддомен `www`:**
   - **Тип:** `A`
   - **Имя:** `www`
   - **Значение:** `89.111.154.27`
   - **TTL:** `3600`

5. **Подожди 5-30 минут** (пока DNS обновится)

6. **Проверь:**
   ```bash
   nslookup твой-домен.ru
   ```
   Должен вернуть `89.111.154.27`

---

## 🔒 Шаг 3: Настрой SSL (HTTPS)

### Вариант 1: Let's Encrypt (бесплатно, автоматически)

1. **Подключись к серверу через PuTTY**

2. **Установи Certbot:**
   ```bash
   apt update
   apt install -y certbot python3-certbot-nginx
   ```

3. **Получи сертификат:**
   ```bash
   certbot --nginx -d твой-домен.ru -d www.твой-домен.ru
   ```
   
   **Важно:** Замени `твой-домен.ru` на свой домен!

4. **Certbot автоматически:**
   - Получит SSL сертификат
   - Настроит Nginx для HTTPS
   - Настроит автоматическое обновление сертификата

5. **Проверь:**
   - Открой `https://твой-домен.ru` в браузере
   - Должен быть зеленый замочек 🔒

---

### Вариант 2: Если домена нет (только IP)

**Проблема:** SSL сертификаты нельзя получить для IP адресов, только для доменов.

**Решения:**
1. Купи домен (от 100-200₽/год на Reg.ru)
2. Или используй сайт по HTTP (без HTTPS)

---

## 📝 Шаг 4: Обнови Nginx конфигурацию для домена

### Если настроил домен, обнови конфигурацию Nginx:

1. **Подключись к серверу через PuTTY**

2. **Отредактируй конфигурацию:**
   ```bash
   nano /etc/nginx/sites-available/diary-app
   ```

3. **Замени `server_name _;` на:**
   ```nginx
   server_name твой-домен.ru www.твой-домен.ru;
   ```

4. **Проверь конфигурацию:**
   ```bash
   nginx -t
   ```

5. **Перезагрузи Nginx:**
   ```bash
   systemctl reload nginx
   ```

---

## 🚀 Шаг 5: Автоматический деплой работает!

### Теперь при каждом изменении:

1. **Внеси изменения в код**
2. **Закоммить и запушь:**
   ```bash
   git add .
   git commit -m "Описание изменений"
   git push origin main
   ```

3. **GitHub Actions автоматически:**
   - Соберет проект
   - Загрузит на сервер
   - Обновит сайт

4. **Проверь GitHub Actions:**
   - Зайди в репозиторий → **Actions**
   - Увидишь статус деплоя

---

## ✅ Чеклист

- [ ] Сайт открывается по IP: `http://89.111.154.27`
- [ ] Все страницы работают
- [ ] Авторизация работает
- [ ] (Опционально) Домен настроен
- [ ] (Опционально) SSL сертификат установлен
- [ ] Автоматический деплой работает

---

## 🐛 Если что-то не работает

### Проблема: Сайт не открывается

**Решение:**
1. Проверь, что Nginx запущен: `systemctl status nginx`
2. Проверь логи: `tail -f /var/log/nginx/error.log`
3. Проверь файлы: `ls -la /var/www/diary-app`

### Проблема: 403 Forbidden

**Решение:**
```bash
chown -R www-data:www-data /var/www/diary-app
chmod -R 755 /var/www/diary-app
```

### Проблема: Домен не работает

**Решение:**
1. Проверь DNS: `nslookup твой-домен.ru`
2. Подожди 30 минут (DNS обновляется не сразу)
3. Проверь конфигурацию Nginx: `nginx -t`

---

## 🎯 Готово!

Твой сайт работает и автоматически обновляется! 🚀

