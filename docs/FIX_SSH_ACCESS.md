# Настройка SSH доступа для подключения с локальной машины

## 🎯 Проблема
Не можешь подключиться к серверу через SSH с локальной машины.

## ✅ Решение: Настрой SSH на сервере через веб-консоль

### Шаг 1: Открой веб-консоль Reg.cloud

### Шаг 2: Выполни эти команды (по одной, они короткие):

```bash
ufw allow 22/tcp
```

```bash
systemctl restart ssh
```

```bash
systemctl status ssh
```

### Шаг 3: Проверь настройки SSH

```bash
grep -E "PasswordAuthentication|PubkeyAuthentication" /etc/ssh/sshd_config
```

Если `PasswordAuthentication no`, включи его:

```bash
sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
```

```bash
systemctl restart ssh
```

---

## 🚀 После настройки SSH

Попробуй подключиться с локальной машины:

```powershell
ssh root@89.111.154.27
```

Пароль: `fYUD4YraHIgw2XUi`

---

## 📝 Альтернатива: Используй GitHub Actions

Если SSH не работает, можно настроить GitHub Actions для автоматического выполнения команд на сервере. Но для этого все равно нужен SSH доступ.

