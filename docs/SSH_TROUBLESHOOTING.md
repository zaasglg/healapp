# Решение проблем с SSH подключением

## 🔍 Проблема: "Permission denied" или "Connection timed out"

### Возможные причины:
1. SSH порт (22) закрыт в firewall
2. SSH доступен только через веб-консоль Reg.cloud
3. Неправильный приватный ключ используется
4. На сервере отключен доступ по паролю

---

## ✅ Решение 1: Использовать веб-консоль Reg.cloud

**Самый надежный способ:**

1. Зайди в панель Reg.cloud
2. Найди свой VPS сервер (IP: 89.111.154.27)
3. Нажми кнопку **"Консоль"** или **"VNC"**
4. Откроется веб-консоль - там выполни все команды

**Это работает всегда!**

---

## ✅ Решение 2: Проверить настройки SSH на сервере

Через веб-консоль выполни:

```bash
# Проверь, запущен ли SSH сервер
systemctl status ssh

# Проверь, разрешен ли доступ по паролю
grep PasswordAuthentication /etc/ssh/sshd_config

# Если нужно включить доступ по паролю (временно)
sed -i 's/#PasswordAuthentication yes/PasswordAuthentication yes/' /etc/ssh/sshd_config
sed -i 's/PasswordAuthentication no/PasswordAuthentication yes/' /etc/ssh/sshd_config
systemctl restart ssh
```

---

## ✅ Решение 3: Использовать правильный ключ при подключении

Если хочешь подключиться через PowerShell с явным указанием ключа:

```powershell
ssh -i "$env:USERPROFILE\.ssh\id_ed25519" root@89.111.154.27
```

---

## ✅ Решение 4: Проверить firewall на сервере

Через веб-консоль:

```bash
# Проверь, открыт ли порт 22
ufw status
# или
iptables -L -n | grep 22

# Если нужно открыть порт
ufw allow 22/tcp
```

---

## 🎯 РЕКОМЕНДАЦИЯ

**Используй веб-консоль Reg.cloud** - это самый простой и надежный способ!

1. Зайди в панель Reg.cloud
2. Открой консоль сервера
3. Выполни команды там

---

## 📝 Команды для установки Nginx (через веб-консоль)

После открытия веб-консоли выполни:

```bash
apt update && apt install -y nginx && systemctl start nginx && systemctl enable nginx && mkdir -p /var/www/diary-app && chown -R www-data:www-data /var/www/diary-app && chmod -R 755 /var/www/diary-app && echo "✅ Готово!"
```
