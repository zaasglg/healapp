# Этап 0: Резервное копирование (КРИТИЧНО!)

## ⚠️ ВАЖНО: Этот этап обязателен перед началом миграции!

**Цель:** Создать полный бэкап текущей БД Supabase, чтобы в случае проблем можно было восстановить все данные.

---

## 📋 Шаг 1: Получение Connection String из Supabase

1. Откройте [Supabase Dashboard](https://app.supabase.com)
2. Выберите ваш проект
3. Перейдите в **Settings** → **Database**
4. Найдите раздел **Connection string**
5. Выберите **URI** (не Pooling)
6. Скопируйте строку подключения

**Пример:**
```
postgresql://postgres:[YOUR-PASSWORD]@db.xxxxx.supabase.co:5432/postgres
```

**⚠️ ВАЖНО:** Замените `[YOUR-PASSWORD]` на реальный пароль БД (его можно найти в Settings → Database → Database password)

---

## 📋 Шаг 2: Установка PostgreSQL Client Tools (если еще не установлены)

### Для Windows:

1. Скачайте PostgreSQL с официального сайта:
   - https://www.postgresql.org/download/windows/
   - Или используйте установщик: https://www.enterprisedb.com/downloads/postgres-postgresql-downloads

2. При установке выберите компоненты:
   - ✅ PostgreSQL Server (опционально, если нужен локальный сервер)
   - ✅ **Command Line Tools** (обязательно!)

3. После установки проверьте:
   ```powershell
   pg_dump --version
   ```

### Альтернатива: Использование Docker

Если не хотите устанавливать PostgreSQL локально, можно использовать Docker:

```powershell
# Создать бэкап через Docker
docker run --rm -e PGPASSWORD="ваш_пароль" postgres:15 pg_dump -h db.xxxxx.supabase.co -U postgres -d postgres > backup.sql
```

---

## 📋 Шаг 3: Создание бэкапа

### Вариант 1: Через скрипт (рекомендуется)

1. Откройте PowerShell в корне проекта
2. Установите переменную окружения с connection string:

   ```powershell
   $env:SUPABASE_DB_URL = "postgresql://postgres:ВАШ_ПАРОЛЬ@db.xxxxx.supabase.co:5432/postgres"
   ```

   **⚠️ ВАЖНО:** Замените `ВАШ_ПАРОЛЬ` и `db.xxxxx.supabase.co` на реальные значения!

3. Запустите скрипт:

   ```powershell
   .\scripts\create-backup.ps1
   ```

4. Бэкап будет сохранен в папку `backups\` с именем `supabase_backup_YYYYMMDD_HHMMSS.sql`

### Вариант 2: Вручную через pg_dump

```powershell
# Установить пароль
$env:PGPASSWORD = "ваш_пароль"

# Создать бэкап
pg_dump -h db.xxxxx.supabase.co -U postgres -d postgres -F c -f backup.sql

# Или в текстовом формате (для проверки)
pg_dump -h db.xxxxx.supabase.co -U postgres -d postgres -f backup.sql
```

### Вариант 3: Через Supabase Dashboard

1. Откройте Supabase Dashboard
2. Перейдите в **Database** → **Backups**
3. Нажмите **Create backup**
4. Дождитесь завершения
5. Скачайте файл бэкапа

**⚠️ ВАЖНО:** Этот метод может не включать все данные (например, auth.users), поэтому рекомендуется использовать pg_dump!

---

## 📋 Шаг 4: Проверка бэкапа

### Через скрипт:

```powershell
.\scripts\verify-backup.ps1 -BackupFile "backups\supabase_backup_YYYYMMDD_HHMMSS.sql"
```

### Вручную:

1. Проверьте размер файла (должен быть больше 0 байт)
2. Откройте файл в текстовом редакторе
3. Убедитесь, что в нем есть:
   - `CREATE TABLE` команды
   - `COPY` команды с данными
   - Комментарии `PostgreSQL database dump`

---

## 📋 Шаг 5: Сохранение бэкапа

**Сохраните файл бэкапа в безопасное место:**

1. ✅ Локальный компьютер (несколько копий)
2. ✅ Облачное хранилище (Google Drive, Dropbox, Yandex Disk)
3. ✅ Внешний жесткий диск (опционально)

**⚠️ ВАЖНО:** 
- НЕ удаляйте бэкап до полной проверки работы нового сервера
- Храните бэкап минимум 1-2 недели после миграции
- Бэкап содержит персональные данные - храните его безопасно!

---

## 📋 Шаг 6: Проверка данных в текущей БД

Перед миграцией полезно проверить, сколько данных у вас есть:

```sql
-- Выполните в Supabase SQL Editor
SELECT 
    'organizations' as table_name, COUNT(*) as count FROM organizations
UNION ALL
SELECT 'clients', COUNT(*) FROM clients
UNION ALL
SELECT 'patient_cards', COUNT(*) FROM patient_cards
UNION ALL
SELECT 'diaries', COUNT(*) FROM diaries
UNION ALL
SELECT 'diary_metrics', COUNT(*) FROM diary_metrics
UNION ALL
SELECT 'organization_employees', COUNT(*) FROM organization_employees
UNION ALL
SELECT 'auth.users', COUNT(*) FROM auth.users
ORDER BY table_name;
```

**Сохраните результаты** - они понадобятся для проверки после импорта!

---

## ✅ Чеклист Этапа 0

- [ ] Получен connection string из Supabase Dashboard
- [ ] Установлены PostgreSQL Client Tools (или используется Docker)
- [ ] Создан бэкап БД (через скрипт или вручную)
- [ ] Проверена целостность бэкапа
- [ ] Бэкап сохранен в безопасное место (несколько копий)
- [ ] Проверено количество записей в основных таблицах
- [ ] Результаты проверки сохранены

---

## 🚀 Следующий шаг

После завершения Этапа 0, **ответьте на вопросы в `docs/MIGRATION_PLAN.md`** и я начну подготовку Этапа 1!

---

## ❓ Проблемы?

Если возникли проблемы:

1. **Ошибка подключения:**
   - Проверьте правильность connection string
   - Проверьте, что IP адрес не заблокирован firewall
   - Убедитесь, что пароль правильный

2. **pg_dump не найден:**
   - Установите PostgreSQL Client Tools
   - Или используйте Docker вариант

3. **Бэкап слишком маленький:**
   - Проверьте, что указали правильную БД
   - Убедитесь, что есть данные в БД
   - Попробуйте создать бэкап через Supabase Dashboard

