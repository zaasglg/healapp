# Этап 1: Подготовка сервера

## 📋 Информация о сервере

- **IP:** `176.124.217.224`
- **SSH:** `ssh root@176.124.217.224`
- **Docker:** ✅ Установлен

---

## 📋 Шаг 1: Подключение к серверу

```bash
ssh root@176.124.217.224
```

**Если возникнут проблемы с подключением:**
- Проверьте, что ваш IP не заблокирован firewall
- Убедитесь, что SSH порт (22) открыт
- Проверьте наличие SSH ключа (если используется)

---

## 📋 Шаг 2: Проверка Docker

После подключения к серверу выполните:

```bash
# Проверить версию Docker
docker --version

# Проверить версию Docker Compose
docker compose version

# Проверить запущенные контейнеры
docker ps

# Проверить все контейнеры (включая остановленные)
docker ps -a
```

**Ожидаемый результат:**
- Docker должен быть установлен и работать
- Docker Compose должен быть доступен
- Могут быть запущены другие контейнеры (это нормально)

**⚠️ ВАЖНО:** Если на сервере уже запущены контейнеры, убедитесь, что они не используют порты, которые нужны для Supabase:
- `5432` (PostgreSQL)
- `8000` (Kong/API)
- `54321` (Supabase API)
- `54324` (Studio)

---

## 📋 Шаг 3: Создание директории для Supabase

```bash
# Создать директорию
mkdir -p ~/supabase
cd ~/supabase

# Создать поддиректории
mkdir -p migrations
mkdir -p functions
mkdir -p backups
```

---

## 📋 Шаг 4: Копирование файлов на сервер

### Вариант 1: Через SCP (с вашего компьютера)

Откройте **новое окно PowerShell** на вашем компьютере (не закрывая SSH сессию):

```powershell
# Перейти в корень проекта
cd "C:\Users\User\Desktop\Веб версия дневник"

# Скопировать docker-compose файл
scp docker-compose.timeweb.yml root@176.124.217.224:~/supabase/docker-compose.yml

# Скопировать kong.yml (если нужен)
scp kong.yml root@176.124.217.224:~/supabase/kong.yml
```

### Вариант 2: Через Git (если проект в репозитории)

На сервере:

```bash
cd ~/supabase
git clone <ваш-репозиторий> .
# Или скопировать только нужные файлы
```

### Вариант 3: Вручную через nano/vim

На сервере создайте файлы вручную (я дам содержимое).

---

## 📋 Шаг 5: Генерация ключей

**На вашем компьютере** выполните:

```powershell
.\scripts\generate-keys.ps1
```

**Сохраните сгенерированные ключи!** Они понадобятся для `.env` файла.

---

## 📋 Шаг 6: Создание .env файла

На сервере создайте файл `.env`:

```bash
cd ~/supabase
nano .env
```

**Содержимое файла** (замените значения на сгенерированные):

```env
# Supabase Configuration
SUPABASE_JWT_SECRET=ваш_jwt_secret_из_скрипта
SUPABASE_ANON_KEY=ваш_anon_key_из_скрипта
SUPABASE_SERVICE_ROLE_KEY=ваш_service_role_key_из_скрипта
SUPABASE_DB_PASSWORD=ваш_db_password_из_скрипта

# Site Configuration
SUPABASE_SITE_URL=https://ваш-домен.ru
# Или если домена нет пока:
# SUPABASE_SITE_URL=http://176.124.217.224:54321

# Ports
SUPABASE_API_PORT=54321
SUPABASE_DB_PORT=54322
SUPABASE_STUDIO_PORT=54324
SUPABASE_FUNCTIONS_PORT=54325
SUPABASE_AUTH_PORT=54326
SUPABASE_REST_PORT=54327
SUPABASE_REALTIME_PORT=54328
SUPABASE_STORAGE_PORT=54329

# URI Allow List (разрешенные домены для CORS)
SUPABASE_URI_ALLOW_LIST=http://176.124.217.224:54321,https://ваш-домен.ru
```

**Сохраните файл:** `Ctrl+O`, `Enter`, `Ctrl+X`

---

## 📋 Шаг 7: Проверка портов

Проверьте, какие порты заняты:

```bash
# Проверить занятые порты
netstat -tuln | grep -E ':(54321|54322|54324|54325|54326|54327|54328|54329)'

# Или через ss
ss -tuln | grep -E ':(54321|54322|54324|54325|54326|54327|54328|54329)'
```

**Если порты заняты:**
- Измените порты в `.env` файле
- Или остановите контейнеры, использующие эти порты (если они не нужны)

---

## 📋 Шаг 8: Настройка Firewall (если нужно)

```bash
# Проверить статус firewall
ufw status

# Если firewall активен, открыть порты
ufw allow 54321/tcp  # API
ufw allow 54324/tcp  # Studio
ufw allow 22/tcp     # SSH (если еще не открыт)
```

**⚠️ ВАЖНО:** Не открывайте порт БД (54322) для внешнего доступа без необходимости!

---

## ✅ Чеклист Этапа 1

- [ ] Подключился к серверу по SSH
- [ ] Проверил Docker и Docker Compose
- [ ] Создал директорию `~/supabase`
- [ ] Скопировал `docker-compose.timeweb.yml` на сервер
- [ ] Сгенерировал ключи на локальном компьютере
- [ ] Создал `.env` файл на сервере с правильными значениями
- [ ] Проверил, что нужные порты свободны
- [ ] Настроил firewall (если нужно)

---

## 🚀 Следующий шаг

После завершения Этапа 1, сообщите мне, и я подготовлю **Этап 2: Развертывание Supabase**.

---

## ❓ Проблемы?

**Не могу подключиться по SSH:**
- Проверьте IP адрес
- Проверьте, что SSH порт открыт
- Попробуйте использовать пароль вместо ключа

**Docker не установлен:**
- Установите Docker: `curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh`
- Добавьте пользователя в группу docker: `usermod -aG docker root`

**Порты заняты:**
- Измените порты в `.env` файле
- Или остановите контейнеры, использующие эти порты

