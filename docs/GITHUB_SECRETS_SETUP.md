# Настройка GitHub Secrets для автоматического деплоя

## 🎯 Что нужно сделать

Добавить секреты в GitHub, чтобы GitHub Actions мог подключаться к серверу и деплоить сайт.

---

## 📝 Шаг 1: Получить приватный SSH ключ

### В PowerShell на твоем компьютере выполни:

```powershell
Get-Content $env:USERPROFILE\.ssh\id_ed25519
```

**Скопируй ВСЁ содержимое** (включая строки `-----BEGIN` и `-----END`)

---

## 🔐 Шаг 2: Добавить секреты в GitHub

1. Зайди в свой репозиторий на GitHub
2. Перейди: **Settings** → **Secrets and variables** → **Actions**
3. Нажми **"New repository secret"**

### Секрет 1: `SSH_HOST`
- **Name**: `SSH_HOST`
- **Value**: `89.111.154.27`
- Нажми **"Add secret"**

### Секрет 2: `SSH_USERNAME`
- **Name**: `SSH_USERNAME`
- **Value**: `root`
- Нажми **"Add secret"**

### Секрет 3: `SSH_PRIVATE_KEY`
- **Name**: `SSH_PRIVATE_KEY`
- **Value**: Вставь **весь** приватный ключ (который скопировал в шаге 1)
- Нажми **"Add secret"**

### Секрет 4: `VITE_SUPABASE_URL`
- **Name**: `VITE_SUPABASE_URL`
- **Value**: URL твоего Supabase проекта (например: `https://xxxxx.supabase.co`)
- Нажми **"Add secret"**

### Секрет 5: `VITE_SUPABASE_ANON_KEY`
- **Name**: `VITE_SUPABASE_ANON_KEY`
- **Value**: Anon key из Supabase Dashboard
- Нажми **"Add secret"**

---

## ✅ Проверка

После добавления всех секретов:
- В разделе Secrets должно быть 5 секретов
- Все секреты должны быть видны (но их значения скрыты)

---

## 🚀 Что дальше

После настройки секретов:
1. Сделай изменения в коде
2. Закоммить и запушь в `main` ветку
3. GitHub Actions автоматически задеплоит сайт!

---

## 📝 Где найти Supabase ключи

1. Зайди в Supabase Dashboard
2. Выбери свой проект
3. Перейди: **Settings** → **API**
4. Скопируй:
   - **Project URL** → `VITE_SUPABASE_URL`
   - **anon public** key → `VITE_SUPABASE_ANON_KEY`

