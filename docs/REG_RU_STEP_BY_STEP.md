# Пошаговая инструкция: Настройка VPS на Reg.ru

## 📍 ТЕКУЩИЙ ЭТАП: Настройка сервера (Пункт 3)

---

## ШАГ 1: Дождаться создания VPS

### Что делать:
1. **На странице "Виртуальные серверы":**
   - Найди свой сервер "Celadon Actinium"
   - Смотри на колонку **"Статус"**
   - Дождись, пока статус изменится с **"Создается"** на **"Активен"** или **"Работает"**

2. **Сколько ждать:**
   - Обычно 2-5 минут
   - Можно обновлять страницу (F5)

3. **Когда готово:**
   - Статус изменится на зеленый "Активен"
   - Можно переходить к следующему шагу

---

## ШАГ 2: Получить данные для доступа

### Что делать:
1. **Нажми на название сервера** ("Celadon Actinium")
   - Откроется страница с деталями

2. **Найди на странице:**
   - **IP-адрес** (например: `185.123.45.67`)
   - **Логин** (обычно `root`)
   - **Пароль** (будет показан или отправлен на email)

3. **Если пароль не виден:**
   - Найди кнопку "Показать пароль" или "Сбросить пароль"
   - Или проверь email `nazardubnak@gmail.com`

4. **Сохрани эти данные:**
   - IP-адрес: `_________________`
   - Логин: `_________________`
   - Пароль: `_________________`

---

## ШАГ 3: Подключиться к серверу

### Windows (PowerShell):

1. **Открой PowerShell:**
   - Нажми `Win + X`
   - Выбери "Windows PowerShell" или "Терминал"

2. **Подключись к серверу:**
   ```powershell
   ssh root@твой_ip_адрес
   ```
   **Замени `твой_ip_адрес` на IP из шага 2!**

3. **При первом подключении:**
   - Появится вопрос: `Are you sure you want to continue connecting (yes/no)?`
   - Введи `yes` и нажми Enter

4. **Введи пароль:**
   - Символы не отображаются при вводе - это нормально!
   - Просто введи пароль и нажми Enter

5. **Если подключился успешно:**
   - Увидишь приглашение: `root@сервер:~#`
   - **Готово! Ты на сервере** ✅

---

## ШАГ 4: Установить Nginx

### На сервере выполни команды (по одной):

```bash
# 1. Обновление системы
apt update && apt upgrade -y
```

**Нажми Enter и жди завершения (1-2 минуты)**

```bash
# 2. Установка Nginx
apt install -y nginx
```

**Нажми Enter и жди завершения (30 секунд)**

```bash
# 3. Запуск Nginx
systemctl start nginx
systemctl enable nginx
```

**Нажми Enter**

```bash
# 4. Проверка статуса
systemctl status nginx
```

**Нажми Enter**

**Ожидаемый результат:**
- Должна появиться надпись `active (running)` зеленым цветом
- Нажми `q` чтобы выйти из просмотра статуса

---

## ШАГ 5: Настроить Nginx для React приложения

### Создай конфигурацию:

```bash
nano /etc/nginx/sites-available/diary-app
```

**Нажми Enter**

### Вставь следующую конфигурацию:

**ВАЖНО:** В nano нужно вставить текст. Скопируй весь текст ниже и вставь в PowerShell (правой кнопкой мыши):

```nginx
server {
    listen 80;
    server_name _;
    
    root /var/www/diary-app;
    index index.html;
    
    # Для React Router (SPA)
    location / {
        try_files $uri $uri/ /index.html;
    }
    
    # Кэширование статических файлов
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }
    
    # Gzip сжатие
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/javascript application/xml+rss application/json;
}
```

### Сохрани файл:
- Нажми `Ctrl + O` (сохранить)
- Нажми `Enter` (подтвердить имя файла)
- Нажми `Ctrl + X` (выйти)

### Активируй конфигурацию:

```bash
# Создаем символическую ссылку
ln -s /etc/nginx/sites-available/diary-app /etc/nginx/sites-enabled/
```

**Нажми Enter**

```bash
# Удаляем дефолтную конфигурацию
rm /etc/nginx/sites-enabled/default
```

**Нажми Enter**

```bash
# Проверяем конфигурацию
nginx -t
```

**Нажми Enter**

**Ожидаемый результат:**
```
nginx: the configuration file /etc/nginx/nginx.conf syntax is ok
nginx: configuration file /etc/nginx/nginx.conf test is successful
```

```bash
# Перезагружаем Nginx
systemctl reload nginx
```

**Нажми Enter**

### Создай директорию для сайта:

```bash
mkdir -p /var/www/diary-app
chown -R www-data:www-data /var/www/diary-app
```

**Нажми Enter**

---

## ШАГ 6: Проверка работы

### Проверь, что Nginx работает:

1. **Открой в браузере:** `http://твой_ip_адрес`
   - Замени `твой_ip_адрес` на IP из шага 2

2. **Должна открыться страница:**
   - "Welcome to nginx!" или пустая страница

**Если видишь эту страницу - Nginx работает!** ✅

---

## ✅ ГОТОВО!

### Что мы сделали:
- ✅ Установили Nginx
- ✅ Настроили конфигурацию для React приложения
- ✅ Создали директорию для файлов сайта
- ✅ Сервер готов к приему файлов

---

## ⏭️ СЛЕДУЮЩИЙ ШАГ: Пункт 4

Теперь нужно настроить GitHub Secrets для автоматического деплоя.

**Но сначала нужно получить:**
1. **VITE_SUPABASE_URL** - URL твоего Supabase проекта
2. **VITE_SUPABASE_ANON_KEY** - Anon key из Supabase Dashboard

---

## 📝 ЗАМЕТКИ

- **Переменные окружения НЕ нужны на сервере** - они нужны в GitHub Secrets
- Сервер только хранит уже собранные файлы
- Сборка происходит в GitHub Actions

---

## ❓ ПРОБЛЕМЫ?

### Не могу подключиться по SSH:
- Проверь, что IP-адрес правильный
- Проверь, что пароль правильный
- Убедись, что сервер в статусе "Активен"

### Ошибка при установке Nginx:
- Убедись, что ты подключен как `root`
- Попробуй: `apt update` еще раз

### Nginx не запускается:
- Проверь конфигурацию: `nginx -t`
- Посмотри логи: `tail -f /var/log/nginx/error.log`

---

**Готово! Теперь переходим к пункту 4!** 🚀

